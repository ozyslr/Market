---
phase: 11-purchase-funnel-guest-checkout
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-purchase-funnel-guest-checkout/UAT-CHECKLIST.md
  - tests/e2e/checkout-guest.spec.ts
  - tests/e2e/checkout-authenticated.spec.ts
  - tests/e2e/checkout-address-book.spec.ts
autonomous: true
requirements: [BUY-05]

must_haves:
  truths:
    - 'UAT-CHECKLIST.md enumerates all 5 carried UAT items with acceptance criteria, tester, date, and sign-off fields'
    - 'Playwright E2E test for guest (anonymous) checkout exercises add-to-cart → delivery → Stripe payment → confirmation'
    - 'Playwright E2E test for authenticated checkout exercises saved-card payment flow'
    - 'Playwright E2E test for address book exercises save-address-at-checkout → select-saved-address flow'
    - 'No new production code is written under BUY-05 — verification artifacts only'
  artifacts:
    - path: '.planning/phases/11-purchase-funnel-guest-checkout/UAT-CHECKLIST.md'
      provides: 'Manual UAT sign-off checklist for all v1.0 carried live-UAT items'
      contains: ['PAY-01', 'PAY-02', 'PAY-03', 'REV-02', 'REV-04']
    - path: 'tests/e2e/checkout-guest.spec.ts'
      provides: 'E2E test for anonymous checkout flow'
      contains: ['guest', 'checkout']
    - path: 'tests/e2e/checkout-authenticated.spec.ts'
      provides: 'E2E test for authenticated checkout with saved card'
      contains: ['authenticated', 'saved card']
    - path: 'tests/e2e/checkout-address-book.spec.ts'
      provides: 'E2E test for address book CRUD + autofill'
      contains: ['address book', 'autofill']
  key_links:
    - from: 'UAT-CHECKLIST.md'
      to: '.planning/v1.0-MILESTONE-AUDIT.md'
      via: 'carried UAT debt items'
      pattern: 'UAT debt'
    - from: 'Playwright E2E tests'
      to: 'Checkout page'
      via: 'browser automation with Stripe mock/test mode'
      pattern: 'page\.goto.*checkout'
---

<objective>
BUY-05: Close v1.0 carried live-UAT debt. This plan produces NO new production code — only verification artifacts. Create a UAT checklist markdown covering the 5 carried UAT items (3DS payments, order lifecycle, shipping, reviews photo UX, Q&A), and write 3 Playwright E2E tests for the Phase 11 checkout flows (guest checkout, authenticated checkout with saved card, address book). All verification, no implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-purchase-funnel-guest-checkout/11-CONTEXT.md
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
From v1.0-MILESTONE-AUDIT.md — carried UAT debt:
- PAY-01/02/03: 3DS payment flows (Iyzico sandbox, Stripe test mode) — code-complete, need live sign-off
- Phase 2: Order lifecycle (create → ship → deliver → review) — code-complete, need live test
- Phase 5: Shipping (Entegi/EasyPost) — code-complete, need live test
- 07-02: Reviews photo upload UX — 2 blocking human-verify checkpoints deferred
- 07-04: Q&A flow — blocking human-verify checkpoint deferred

From playwright.config.ts (root):

```typescript
// Playwright 1.60.0 config at project root
// Existing test patterns: tests/e2e/ directory
```

From Checkout.tsx:

```typescript
// Mock mode: const [isMock, setIsMock] = useState(false)
// When isMock is true, StripePaymentForm simulates payment (no real Stripe API call)
// For E2E: tests can run in mock mode or against Stripe test keys
```

Key constraint: E2E tests should work with mock mode (isMock=true) so they run in CI without external Stripe dependency. Stripe test mode can be a separate CI workflow.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UAT checklist markdown</name>
  <files>.planning/phases/11-purchase-funnel-guest-checkout/UAT-CHECKLIST.md</files>
  <read_first>
    - .planning/v1.0-MILESTONE-AUDIT.md (full file — understand the 5 UAT debt items and their context)
    - 11-CONTEXT.md (UAT debt section — lines 44-51)
  </read_first>
  <action>
    Per D-BUY-06, create a manual UAT sign-off checklist:

    **UAT-CHECKLIST.md** structure:

    ```markdown
    # v1.0 Live UAT Sign-Off Checklist — Phase 11 Closure

    > Generated: 2026-06-05 | Phase: 11-purchase-funnel-guest-checkout | BUY-05

    ## Instructions

    For each item: follow the test steps on the live/staging environment. Check the box when the observed behavior matches the expected result. Sign with date and initials.

    ## 1. PAY-01/02/03 — 3DS Payment Flows

    ### PAY-01: Stripe 3DS Payment
    - [ ] Add item to cart → checkout with Stripe → use 3DS test card (4000002500003155)
    - [ ] 3DS challenge modal appears → complete authentication
    - [ ] Payment succeeds → order created → order confirmation email received

    ### PAY-02: Iyzico 3DS Payment (Sandbox)
    - [ ] Add item to cart → checkout with Iyzico → complete 3DS flow in sandbox
    - [ ] Payment callback returns success → order created

    ### PAY-03: Payment Failure Recovery
    - [ ] Use decline card (4000000000000002) → decline message shown → user can retry with different method
    - [ ] Switch to successful card → order created

    ## 2. Phase 2 — Order Lifecycle

    - [ ] Place an order → verify order appears in seller dashboard "Orders" tab
    - [ ] Seller updates status: Processing → Shipped → verify buyer sees updated status
    - [ ] Seller marks as Delivered → buyer receives delivery notification
    - [ ] Buyer leaves a review after delivery → review appears on product page

    ## 3. Phase 5 — Shipping (Entegi/EasyPost)

    - [ ] Place an order with valid TR shipping address → shipping rate calculated at checkout
    - [ ] Seller generates shipping label in dashboard → label displays correctly
    - [ ] Tracking number updates are reflected in buyer order detail

    ## 4. 07-02 — Reviews Photo Upload UX

    - [ ] Buyer writes a review → clicks "Add Photo" → selects 1-5 images
    - [ ] Photos upload with progress indicator → appear as thumbnails in review form
    - [ ] Submit review with photos → photos appear on product page review card
    - [ ] Click photo thumbnail → lightbox/viewer opens

    ## 5. 07-04 — Q&A Flow

    - [ ] Buyer submits a question on product page → question appears with "pending answer" badge
    - [ ] Seller receives notification → seller answers question → answer appears on product page
    - [ ] Question/answer thread is visible to other buyers browsing the product

    ## Sign-Off

    | Item | Tester | Date | Result | Notes |
    |------|--------|------|--------|-------|
    | PAY-01/02/03 (3DS) | | | ⬜ Pass / ⬜ Fail | |
    | Phase 2 (Order Lifecycle) | | | ⬜ Pass / ⬜ Fail | |
    | Phase 5 (Shipping) | | | ⬜ Pass / ⬜ Fail | |
    | 07-02 (Photo UX) | | | ⬜ Pass / ⬜ Fail | |
    | 07-04 (Q&A) | | | ⬜ Pass / ⬜ Fail | |

    ## Phase 11 New Feature UAT

    | Item | Tester | Date | Result | Notes |
    |------|--------|------|--------|-------|
    | BUY-01 (Guest Checkout) | | | ⬜ Pass / ⬜ Fail | |
    | BUY-02 (Address Book) | | | ⬜ Pass / ⬜ Fail | |
    | BUY-03 (Express Wallets) | | | ⬜ Pass / ⬜ Fail | Requires HTTPS |
    | BUY-04 (Funnel Analytics) | | | ⬜ Pass / ⬜ Fail | |
    ```

    2. Include practical notes for each section: required test cards, sandbox URLs, known constraints.
    3. Add a "Prerequisites" section at the top listing: deployment environment, test accounts (buyer + seller), test cards, and required browser/device configurations.

  </action>
  <verify><automated>File exists at .planning/phases/11-purchase-funnel-guest-checkout/UAT-CHECKLIST.md with all 5 carried-UAT sections + Phase 11 new feature sections</automated></verify>
  <done>UAT-CHECKLIST.md enumerates all carried v1.0 UAT items and Phase 11 features with acceptance criteria, test steps, and sign-off table.</done>
</task>

<task type="auto">
  <name>Task 2: Write Playwright E2E tests for checkout flows</name>
  <files>tests/e2e/checkout-guest.spec.ts, tests/e2e/checkout-authenticated.spec.ts, tests/e2e/checkout-address-book.spec.ts</files>
  <read_first>
    - playwright.config.ts (root — understand baseURL, testDir, projects config)
    - tests/e2e/ directory — find an existing test file to understand patterns, page objects, and test conventions
    - src/pages/Checkout.tsx (the mock mode logic — isMock flag, how to trigger it)
  </read_first>
  <action>
    Create 3 Playwright E2E test files per D-BUY-06:

    **1. `tests/e2e/checkout-guest.spec.ts` — Guest checkout flow:**
    - Test: "Guest completes checkout from cart to order confirmation"
      a. Navigate to home page (anonymous — no sign-in).
      b. Add first available product to cart (click "Sepete Ekle" button).
      c. Open cart drawer, click "Sepeti Onayla" → navigates to /checkout.
      d. Step 1 (Delivery): fill guest email field, fill all required address fields (fullName, line1, city, postalCode, phone), click "Devam".
      e. Step 2 (Payment): verify Stripe form renders, click "Öde" (mock mode auto-completes), wait for confirmation.
      f. Step 3 (Confirmation): verify order confirmation message visible, verify "Hesap Oluştur" prompt card renders.
    - Test: "Guest checkout email validation"
      a. Leave email field empty → click "Devam" → verify validation error.
      b. Enter invalid email → verify validation error.
    - Test: "Guest cart persists across page refresh"
      a. Add item to cart → refresh page → verify item still in cart.

    **2. `tests/e2e/checkout-authenticated.spec.ts` — Authenticated checkout:**
    - Test: "Authenticated user checks out with saved card"
      a. Sign in with test account (use Playwright storageState or inline Firebase auth).
      b. Navigate to /checkout with items in cart.
      c. Step 1: verify saved addresses shown, select first address.
      d. Step 2: verify saved cards shown, select first card, click "Öde".
      e. Step 3: verify confirmation with "Siparişin Alındı" message.
    - Test: "Authenticated user checks out with new card (Stripe Elements)"
      a. Sign in, navigate to /checkout, fill address.
      b. Step 2: fill Stripe test card (4242 4242 4242 4242) via Stripe Elements iframe.
      c. Verify payment succeeds.

    **3. `tests/e2e/checkout-address-book.spec.ts` — Address book flow:**
    - Test: "Save new address at checkout and reuse"
      a. Sign in with test account.
      b. Navigate to /checkout → Step 1.
      c. Toggle "Yeni Adres" form, fill all fields, select address type "Ev".
      d. Check "Bu adresi profilime kaydet" checkbox.
      e. Continue through payment (mock mode).
      f. After order: navigate to /checkout again → verify saved address appears in "Kayıtlı Adreslerim".
      g. Click saved address → verify form auto-fills.
    - Test: "Address type badges render correctly"
      a. With addresses of different types saved → open checkout → verify "Ev"/"İş"/"Diğer" badges on address cards.

    **Test conventions to follow:**
    - Use `test.describe` / `test` from `@playwright/test`.
    - Use `page.goto('/checkout')` (baseURL configured in playwright.config).
    - Use `page.locator('[data-testid="..."]')` selectors where available; fall back to text content selectors.
    - Add `test.slow()` annotation for checkout tests (multi-step flows).
    - All tests run against mock mode (no real Stripe API calls) — set isMock in the app or intercept the Stripe script.
    - If the app doesn't expose a mock toggle via URL param, note in test comments that `VITE_STRIPE_PUBLISHABLE_KEY` must be set to a test key or mock mode must be enabled.
    - Use `page.waitForURL('**/checkout')` for navigation assertions.

  </action>
  <verify><automated>cd "O:/AI/E-tic 2026" && npx playwright test tests/e2e/checkout-guest.spec.ts tests/e2e/checkout-authenticated.spec.ts tests/e2e/checkout-address-book.spec.ts --list</automated></verify>
  <done>3 Playwright E2E test files created: guest checkout flow, authenticated checkout with saved card, address book CRUD. All tests list successfully.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                                                                           |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| Test→Stripe              | E2E tests use mock mode — no real Stripe API calls, no real payment data                                              |
| Test→Firebase            | E2E tests may use test Firebase project or emulator; no production data affected                                      |
| UAT checklist→Production | Manual UAT items require live environment — tester must ensure test accounts/cards are used, not real payment methods |

## STRIDE Threat Register

| Threat ID | Category               | Component                            | Disposition | Mitigation Plan                                                                                                        |
| --------- | ---------------------- | ------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-11-14   | Information Disclosure | UAT checklist with test card numbers | accept      | Test card numbers are publicly documented by Stripe for testing. No real credentials in checklist.                     |
| T-11-15   | Tampering              | E2E test mock mode                   | accept      | Mock mode is for testing only; production never runs in mock mode. No security impact.                                 |
| T-11-16   | Elevation              | Playwright tests authenticating      | mitigate    | Use test Firebase project (or emulator) with dedicated test accounts. Never use production credentials in test config. |

</threat_model>

<verification>
- UAT-CHECKLIST.md exists with all 5 carried-UAT sections + Phase 11 sections + sign-off table
- `npx playwright test --list` lists all 3 new test files
- `npx playwright test tests/e2e/checkout-guest.spec.ts --dry-run` succeeds (no config errors)
- Manual: Review UAT-CHECKLIST.md for completeness against v1.0-MILESTONE-AUDIT.md
</verification>

<success_criteria>
UAT checklist covers all v1.0 carried live-UAT items with clear test steps and sign-off fields. Three Playwright E2E tests cover the critical checkout flows: guest, authenticated with saved card, and address book.
</success_criteria>

<output>Create `.planning/phases/11-purchase-funnel-guest-checkout/11-05-SUMMARY.md` when done.</output>
