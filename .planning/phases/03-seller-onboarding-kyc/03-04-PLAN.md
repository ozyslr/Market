---
phase: 03-seller-onboarding-kyc
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - server/routes/sellerApi.ts
  - src/services/apiKeyService.ts
  - src/pages/SellerApiKeys.tsx
autonomous: true
requirements:
  - SEL-06

must_haves:
  truths:
    - "D-10: API key hashing uses SHA-256 (createHash('sha256')) in both sellerApi.ts and apiKeyService.ts — the broken djb2 hash is removed"
    - 'Key comparison uses crypto.timingSafeEqual (constant-time) — direct string equality is removed'
    - 'Existing active API keys in Firestore are invalidated (old hash incompatible) and sellers are notified'
    - 'Rate limiting in sellerApi.ts uses Firestore-backed counter (collection apiRateLimits) instead of in-memory Map'
    - 'sellerApi.ts has no mojibake UTF-8 encoding errors in Turkish string literals'
    - 'SellerApiKeys.tsx shows raw key ONCE at creation in a highlighted box with copy button; raw key is never stored in Firestore'
    - 'SellerApiKeys.tsx revoke flow uses inline confirmation (not modal)'
  artifacts:
    - path: 'server/routes/sellerApi.ts'
      provides: 'SHA-256 key hash + timingSafeEqual + Firestore rate limit + UTF-8 fixed'
      contains: 'timingSafeEqual'
    - path: 'src/services/apiKeyService.ts'
      provides: 'hashApiKey using SHA-256 via server API call; invalidation flow for existing keys'
      contains: 'sha256'
    - path: 'src/pages/SellerApiKeys.tsx'
      provides: 'Raw key shown once, copy flow, revoke inline confirmation per UI-SPEC Surface 6'
      contains: 'will not be shown again'
  key_links:
    - from: 'src/pages/SellerApiKeys.tsx'
      to: 'POST /api/v1/keys (server-side key creation)'
      via: 'API call that returns { rawKey } once'
      pattern: 'rawKey'
    - from: 'server/routes/sellerApi.ts'
      to: "adminDb.collection('apiRateLimits')"
      via: 'Firestore counter with FieldValue.increment(1)'
      pattern: 'apiRateLimits'
---

<objective>
Seller REST API security hardening: replace broken 32-bit djb2 hash with SHA-256 + constant-time comparison, replace in-memory Map rate limiting with Firestore-backed counter, fix UTF-8 mojibake, invalidate incompatible existing keys. Update SellerApiKeys.tsx UI to show raw key once and implement inline revoke confirmation per UI-SPEC.

Purpose: The existing hash is brute-forceable (32-bit space, ~4 billion values). Timing attacks can extract the hash bit-by-bit via direct equality. In-memory rate limits reset on server restart. These are high-severity security gaps per D-10.
Output: Hardened sellerApi.ts, updated apiKeyService.ts (server-side hash), hardened SellerApiKeys.tsx (raw-key-once UX + inline revoke).
</objective>

<execution_context>
@O:/AI/E-tic 2026/.planning/phases/03-seller-onboarding-kyc/03-PATTERNS.md
</execution_context>

<context>
@O:/AI/E-tic 2026/.planning/ROADMAP.md
@O:/AI/E-tic 2026/.planning/phases/03-seller-onboarding-kyc/03-CONTEXT.md
@O:/AI/E-tic 2026/.planning/phases/03-seller-onboarding-kyc/03-RESEARCH.md
@O:/AI/E-tic 2026/.planning/phases/03-seller-onboarding-kyc/03-UI-SPEC.md

<interfaces>
<!-- Key contracts executor needs -->

From Node.js crypto (built-in, no install):

- import { createHash, timingSafeEqual } from 'crypto'
- createHash('sha256').update(rawKey, 'utf8').digest('hex') -> 64-char hex string
- timingSafeEqual(Buffer.from(hashA), Buffer.from(hashB)) -> boolean (throws if lengths differ)

From server/routes/sellerApi.ts (current — to harden):

- Line 26: broken hashApiKey function using (h<<5)-h+charCodeAt — REPLACE entirely
- Line 18: new Map() rate limiting — REPLACE with Firestore counter
- Lines 1, 53, 56, 58+: mojibake Turkish strings — FIX to correct UTF-8
- authenticateApiKey(apiKey): currently calls broken hashApiKey — update to timingSafeEqual

From src/services/apiKeyService.ts (current — to update):

- Line 68-77: broken hashKey() function — SHA-256 must be computed server-side
- Line 205: new Map() rate limiting (client-side mirror) — remove if present
- createApiKey(name, permissions): must call server POST /api/v1/keys to get { rawKey, keyId }
- The raw key is shown once at creation; only SHA-256 hash stored in Firestore

Firestore rate limit document shape (new collection apiRateLimits):

- Document ID: "{sellerId}:{permission}"
- Fields: { count: number, resetAt: number (epoch ms), windowMs: 60000, max: 100 }
- Increment: FieldValue.increment(1) in transaction
- Reset: if Date.now() > resetAt, reset count=1 and resetAt=Date.now()+windowMs

From src/pages/SellerApiKeys.tsx (current — to harden UI):

- Existing create/list/revoke handlers
- "Raw key shown once" pattern: after creation, set rawKey state, render highlighted box
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: sellerApi.ts — SHA-256 hash + timingSafeEqual + Firestore rate limit + UTF-8 fix</name>
  <files>
    server/routes/sellerApi.ts
  </files>
  <read_first>
    - server/routes/sellerApi.ts (read full — all lines: broken hashApiKey at line 26, new Map() at line 18, mojibake strings at lines 1/53/56/58, authenticateApiKey function, all route handlers, existing Firestore write patterns, how sellerId and permissions are accessed per-request)
    - server/routes/iyzico.ts (read lines 1-15 — how adminDb is passed via deps; confirm Firestore import pattern for FieldValue)
  </read_first>
  <action>
    Rewrite server/routes/sellerApi.ts with these targeted changes (preserve all existing route logic, only replace the hash, rate limit, and fix encoding):

    HASH REPLACEMENT: Remove the entire broken hashApiKey function (lines 68-77 area, the (h<<5)-h+charCodeAt implementation). Replace with:
      import { createHash, timingSafeEqual } from 'crypto' at top of file.
      function hashApiKey(rawKey: string): string { return createHash('sha256').update(rawKey, 'utf8').digest('hex'); }
      function verifyApiKey(rawKey: string, storedHash: string): boolean { const a = Buffer.from(hashApiKey(rawKey)); const b = Buffer.from(storedHash, 'hex'); if (a.length !== b.length) return false; return timingSafeEqual(a, b); }
    Update authenticateApiKey to call verifyApiKey(rawKey, doc.data().keyHash) instead of direct equality or old hashApiKey.

    RATE LIMIT REPLACEMENT: Remove the in-memory Map (const rateLimitMap = new Map() or similar at line 18). Replace with an async Firestore-backed rate limit check:
      async function checkApiRateLimit(adminDb: Firestore, sellerId: string, permission: string): Promise<{ allowed: boolean; remaining: number }> {
        const key = `${sellerId}:${permission}`;
        const ref = adminDb.collection('apiRateLimits').doc(key);
        const MAX = 100; const WINDOW = 60_000;
        return adminDb.runTransaction(async (tx) => {
          const snap = await tx.get(ref);
          const now = Date.now();
          if (!snap.exists || now > (snap.data()?.resetAt ?? 0)) {
            tx.set(ref, { count: 1, resetAt: now + WINDOW, max: MAX, windowMs: WINDOW });
            return { allowed: true, remaining: MAX - 1 };
          }
          const count = snap.data()!.count;
          if (count >= MAX) return { allowed: false, remaining: 0 };
          tx.update(ref, { count: FieldValue.increment(1) });
          return { allowed: true, remaining: MAX - count - 1 };
        });
      }
    Import FieldValue from 'firebase-admin/firestore'. Update all rate limit checks to await checkApiRateLimit(adminDb, sellerId, permission). Return 429 with { error: 'Rate limit exceeded. Try again in 60 seconds.' } when allowed === false.

    UTF-8 FIX: Scan every string literal in the file. Replace all mojibake sequences (characters like â€", Ã¼, Ä°, etc. that appear where Turkish characters should be) with their correct UTF-8 equivalents. Common replacements: geçersiz API anahtarı, İzin verilmedi, Hız limiti aşıldı. This is a string-only fix — no logic changes. Ensure the file is saved as UTF-8.

    KEY INVALIDATION: Add a one-time migration step. At the start of registerSellerApiRoutes (or in a startup hook), log a warning: 'MIGRATION REQUIRED: existing API keys hashed with old algorithm are now invalid. Sellers must regenerate their API keys.' Also update the authenticateApiKey function to return a specific error message when key hash length != 64 chars (old hash was 8 chars): return 401 with { error: 'Your API key is no longer valid. Please generate a new key from your dashboard.', code: 'KEY_REHASH_REQUIRED' }.

    Do NOT change any route paths, permissions logic, or Firestore collection names beyond apiRateLimits.

  </action>
  <verify>
    <automated>cd "O:/AI/E-tic 2026" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -v "^//" server/routes/sellerApi.ts | grep -c "timingSafeEqual" outputs >= 1 (timingSafeEqual used in production code, not just comments)
    - grep -v "^//" server/routes/sellerApi.ts | grep -c "h<<5\|charCodeAt\|hashKey.*for" outputs 0 (old djb2 hash gone)
    - grep -v "^//" server/routes/sellerApi.ts | grep -c "new Map()" outputs 0 (in-memory Map removed)
    - grep -n "apiRateLimits" server/routes/sellerApi.ts outputs >= 1 line (Firestore rate limit collection)
    - grep -n "FieldValue.increment" server/routes/sellerApi.ts outputs >= 1 line
    - File saved as UTF-8 (no mojibake sequences visible when read back)
    - tsc --noEmit passes with no new errors from this file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: apiKeyService.ts server-side hash + SellerApiKeys.tsx raw-key-once UX + inline revoke</name>
  <files>
    src/services/apiKeyService.ts,
    src/pages/SellerApiKeys.tsx
  </files>
  <read_first>
    - src/services/apiKeyService.ts (read full — existing hashKey function lines 68-77, createApiKey function, ApiKeyPermission types, Firestore write pattern, how raw key is currently generated and stored)
    - src/pages/SellerApiKeys.tsx (read full — existing UI structure, create flow, key list rendering, revoke handler, useAuth, motion imports)
    - src/index.css (read lines 1-60 — accent, yellow-50/amber CSS token values for raw key highlight box)
  </read_first>
  <action>
    In src/services/apiKeyService.ts: the client service MUST NOT contain a SHA-256 hash implementation (Node crypto is not available in the browser). Refactor createApiKey to call a server endpoint instead of hashing client-side:

    Remove the broken hashKey function entirely (lines 68-77). Add a comment: "Hash computed server-side only — never hash API keys in browser code."

    Update createApiKey(name: string, permissions: ApiKeyPermission[]): call POST /api/v1/keys with { name, permissions } and Authorization: Bearer ${await user.getIdToken()}. The server generates the raw key (crypto.randomBytes(32).toString('hex') with 'bo_' prefix), hashes it with SHA-256, stores only the hash in Firestore. The server returns { rawKey, keyId } once. The client stores rawKey in component state only (never in Firestore, never in localStorage).

    Add server endpoint POST /api/v1/keys inside registerSellerApiRoutes in server/routes/sellerApi.ts (add to Task 1's file): verifyFirebaseToken, body: { name, permissions }. Generate rawKey = 'bo_' + crypto.randomBytes(32).toString('hex'). Hash: keyHash = hashApiKey(rawKey). Write to Firestore apiKeys/{keyId}: { keyHash, name, permissions, sellerId:req.uid, createdAt, lastUsed:null }. Return { rawKey, keyId } — this is the ONLY time rawKey leaves the server.

    Remove in-memory Map rate limiting from apiKeyService.ts if present (client-side rate limit is redundant with server-side Firestore check).

    In src/pages/SellerApiKeys.tsx: implement the UI per UI-SPEC Surface 6:

    Create key flow: "Create API Key" button (accent fill, Plus icon left). Inline form expands below (motion.div initial={{ height:0, opacity:0 }} animate={{ height:'auto', opacity:1 }} 200ms ease-out). Form has: key name input, checkbox grid for permissions (one checkbox per ApiKeyPermission scope). On submit: call apiKeyService.createApiKey(name, permissions) -> get { rawKey, keyId }. Set state: newlyCreatedKey = rawKey.

    Raw key display: render highlighted box immediately after creation — yellow-50 bg, amber-300 border, rounded-lg p-4. Monospace font (font-mono text-sm) for key value. "Copy Key" button (accent outline). Warning text: "This key will not be shown again. Copy it now." (amber-700 text-sm, AlertCircle icon). After copy click OR after user clicks "I've saved it" dismiss button: set newlyCreatedKey = null, key is hidden and masked in table.

    Key list table columns per UI-SPEC: Key name, Prefix (first 8 chars + "..."), Permissions (scope pills: gray-100 bg text-gray-700 text-xs rounded px-2 py-0.5), Created, Last used, Actions (Revoke button).

    Rate limit info row: below table, gray-50 card, Info icon 14px gray-400, "API requests are limited to 100 per minute per key. Limits reset every 60 seconds." text-sm text-gray-500.

    Revoke flow: inline confirmation (not modal). Clicking "Revoke" button (text-red-600, XCircle icon) on a row expands an inline confirmation div below that row (AnimatePresence + motion.div exit={{ opacity:0, height:0 }} 300ms). Text: "Revoke this key? This cannot be undone." Red "Revoke Key" button + gray "Cancel" link. On confirm: call revokeApiKey(keyId) (existing service function or new DELETE /api/v1/keys/:id call). Revoked row fades out: motion exit={{ opacity:0, height:0 }} 300ms ease-in, removed from list after animation.

    All copy strings must match exactly the UI-SPEC Copywriting Contract surface 6 entries. Use logical Tailwind properties (ms-, me-) for RTL compatibility.

  </action>
  <verify>
    <automated>cd "O:/AI/E-tic 2026" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -v "^//" src/services/apiKeyService.ts | grep -c "h<<5\|charCodeAt\|hashKey.*for" outputs 0 (old hash gone from client service)
    - grep -n "will not be shown again" src/pages/SellerApiKeys.tsx outputs >= 1 line (raw key warning present)
    - grep -n "newlyCreatedKey\|rawKey" src/pages/SellerApiKeys.tsx outputs >= 1 line (raw key held in component state)
    - grep -n "localStorage\|sessionStorage" src/pages/SellerApiKeys.tsx outputs 0 lines (raw key never persisted to storage)
    - grep -n "AnimatePresence\|exit=" src/pages/SellerApiKeys.tsx outputs >= 1 line (revoke fade-out animation)
    - grep -n "Revoke this key" src/pages/SellerApiKeys.tsx outputs >= 1 line (inline revoke confirmation text)
    - grep -n "apiRateLimits\|timingSafeEqual" server/routes/sellerApi.ts outputs >= 2 lines (from Task 1, confirming both hardening changes still present)
    - tsc --noEmit passes with no new errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                |
| ----------------------------------------- | ------------------------------------------------------------------------------------------ |
| External API client → GET/POST /api/v1/\* | API key in X-API-Key header; SHA-256 hash + timingSafeEqual before any data access         |
| Server rate limit state                   | Firestore-backed (survives restarts); atomic FieldValue.increment prevents race conditions |
| Key creation                              | Raw key returned once from server; only SHA-256 hash stored in Firestore                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                                    | Disposition | Mitigation Plan                                                                                                      |
| --------- | ---------------------- | -------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| T-03-17   | Spoofing               | API key brute force via timing attack        | mitigate    | Replace direct equality with crypto.timingSafeEqual; 256-bit SHA-256 hash space vs old 32-bit djb2                   |
| T-03-18   | Spoofing               | API key hash collision (djb2 32-bit)         | mitigate    | Replace djb2 with SHA-256 (createHash('sha256')); invalidate all existing keys on deploy                             |
| T-03-19   | Denial of Service      | API key rate limit bypass on server restart  | mitigate    | Replace in-memory Map with Firestore-backed counter (persistent across restarts); atomic increment via transaction   |
| T-03-20   | Information Disclosure | Raw API key leaked via Firestore             | mitigate    | Only keyHash (SHA-256) stored in Firestore; rawKey returned from server once and never persisted on client           |
| T-03-21   | Information Disclosure | Raw API key leaked via localStorage          | mitigate    | SellerApiKeys.tsx holds rawKey in component state only; no localStorage/sessionStorage writes                        |
| T-03-22   | Tampering              | Old-hash keys still accepted after migration | mitigate    | authenticateApiKey returns 401 with KEY_REHASH_REQUIRED code if storedHash.length != 64 (old 8-char hashes rejected) |
| T-03-SC   | Tampering              | npm/pip/cargo installs                       | accept      | No new packages — Node.js built-in crypto module used                                                                |

</threat_model>

<verification>
After both tasks complete:

1. `npx tsc --noEmit` — zero new errors
2. Grep check: `grep -v "^//" server/routes/sellerApi.ts | grep "h<<5"` — must return no output
3. Grep check: `grep -v "^//" server/routes/sellerApi.ts | grep "new Map()"` — must return no output
4. Grep check: `grep "timingSafeEqual" server/routes/sellerApi.ts` — must return >= 1 line
5. Manual: create new API key via dashboard — raw key shown once in yellow-50 box with copy button; after dismissal, key shows masked in table
6. Manual: attempt to use an old-format API key (8 char hash) — server returns 401 with code: 'KEY_REHASH_REQUIRED'
7. Manual: send 101 requests within 60s to any /api/v1/ endpoint — 101st returns 429 "Rate limit exceeded"
8. Manual check: open server/routes/sellerApi.ts in editor — no garbled Turkish characters visible in string literals
   </verification>

<success_criteria>

- SHA-256 + timingSafeEqual in sellerApi.ts — old djb2 hash completely removed
- Firestore-backed rate limit (apiRateLimits collection) replaces in-memory Map
- UTF-8 encoding correct in all string literals in sellerApi.ts
- Existing old-hash API keys rejected with KEY_REHASH_REQUIRED error
- Raw API key shown once in UI, never written to Firestore or localStorage
- Inline revoke confirmation (no modal) with AnimatePresence fade-out
- tsc --noEmit: zero new errors
  </success_criteria>

<output>
Create `.planning/phases/03-seller-onboarding-kyc/03-04-SUMMARY.md` when done.
</output>
