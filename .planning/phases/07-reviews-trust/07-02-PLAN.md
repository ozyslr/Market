---
phase: 07-reviews-trust
plan: 02
type: execute
wave: 2
depends_on: ['07-01']
files_modified:
  - src/components/product/ReviewForm.tsx
  - src/components/product/ReviewCard.tsx
  - src/components/product/ReviewSection.tsx
autonomous: true
requirements: [REV-02]
user_setup: []

must_haves:
  truths:
    - 'A buyer can attach up to 5 photos to a review; the 6th is rejected client-side'
    - 'Uploaded photos appear in a gallery on the product page review'
    - 'Clicking a review photo opens a lightbox'
  artifacts:
    - path: 'src/components/product/ReviewCard.tsx'
      provides: 'review photo gallery + lightbox'
      contains: 'photos'
    - path: 'src/components/product/ReviewForm.tsx'
      provides: 'max-5 photo upload via uploadReviewPhoto'
      contains: 'uploadReviewPhoto'
  key_links:
    - from: 'src/components/product/ReviewForm.tsx'
      to: 'uploadReviewPhoto'
      via: 'Firebase Storage upload returning URLs sent in /api/reviews photos[]'
      pattern: 'uploadReviewPhoto'
    - from: 'src/components/product/ReviewCard.tsx'
      to: 'review.photos'
      via: 'map render with lightbox index state'
      pattern: 'photos'
---

<objective>
Vertical slice for REV-02: buyers attach up to 5 photos to a review and view them in a product-page gallery with a lightbox. Photos upload via the existing `uploadReviewPhoto` (Firebase Storage); the resulting URLs are sent in the `photos[]` field of the POST /api/reviews call from 07-01 (server already validates max 5 via Zod).

Purpose: Adds visual trust to reviews (D-discretion REV-02). Builds on the 07-01 submit path.
Output: ReviewForm photo picker (max 5), ReviewCard gallery + lightbox.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reviews-trust/07-CONTEXT.md
@.planning/phases/07-reviews-trust/07-PATTERNS.md
@.planning/phases/07-reviews-trust/07-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- From 07-PATTERNS.md -->
src/services/reviewService.ts: uploadReviewPhoto (lines 193-203) — already implemented Firebase Storage upload, returns a download URL. Enforce max-5 on the client before calling.
src/services/reviewService.ts: submitReview(payload) — added in 07-01; payload.photos is string[] (URLs). Server validates max 5.
ReviewCard / ReviewForm: named export + local Props interface (CLAUDE.md convention). Lightbox via useState index, no new library.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Photo upload (max 5) in ReviewForm</name>
  <files>src/components/product/ReviewForm.tsx</files>
  <read_first>
    - src/components/product/ReviewForm.tsx (current submit handler; the submitReview call added in 07-01)
    - src/services/reviewService.ts (uploadReviewPhoto signature lines 193-203)
    - .planning/phases/07-reviews-trust/07-PATTERNS.md § reviewService line 94 (max-5 guard) and § UI components
    - .planning/phases/07-reviews-trust/07-01-SUMMARY.md (the submitReview payload shape)
  </read_first>
  <action>
    Add a file input (accept image/*, multiple) to ReviewForm. Maintain a local photos URL array state. On selection: reject if it would exceed 5 total (show Turkish error "En fazla 5 fotoğraf ekleyebilirsiniz."); for each accepted file call uploadReviewPhoto and append the returned URL; show per-file upload progress/disabled state and a remove-photo control. On submit, pass the photos URL array in the submitReview payload's photos field. Validate content is an image and reasonable size before upload (reject non-image types).
  </action>
  <verify>
    <automated>cd "O:/AI/E-tic 2026" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ReviewForm calls uploadReviewPhoto and collects returned URLs into a photos array capped at 5.
    - Selecting a 6th photo is blocked with the Turkish max-5 message; non-image files rejected.
    - submitReview payload includes photos: string[].
    - tsc --noEmit passes.
  </acceptance_criteria>
  <done>Buyer can attach up to 5 image photos that upload to Storage and are submitted with the review; over-limit and non-image rejected.</done>
</task>

<task type="auto">
  <name>Task 2: Photo gallery + lightbox in ReviewCard and ReviewSection</name>
  <files>src/components/product/ReviewCard.tsx, src/components/product/ReviewSection.tsx</files>
  <read_first>
    - src/components/product/ReviewCard.tsx (verified badge from 07-01; where to render photos)
    - src/components/product/ReviewSection.tsx (how ReviewCard is rendered in the list)
    - .planning/phases/07-reviews-trust/07-PATTERNS.md § ReviewCard photo gallery line 330 (lightbox via useState index)
  </read_first>
  <action>
    In ReviewCard.tsx render review.photos[] as a thumbnail row (img tags) when present. Add a lightbox: useState for the open photo index (null when closed); clicking a thumbnail sets the index; an overlay shows the full image with prev/next and a close control; clicking the backdrop or Escape closes. No new library — plain state + fixed-position overlay. Ensure ReviewSection passes through review objects unchanged (photos already on the Review type). Keep accessibility: alt text and a focusable close button.
  </action>
  <verify>
    <automated>cd "O:/AI/E-tic 2026" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ReviewCard renders review.photos thumbnails when the array is non-empty.
    - Clicking a thumbnail opens a lightbox overlay; Escape/backdrop closes it.
    - tsc --noEmit passes.
  </acceptance_criteria>
  <done>Review photos display as a gallery on the product page with a working lightbox.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Photo upload (max 5) in the review form and a photo gallery + lightbox on product-page reviews.</what-built>
  <how-to-verify>
    1. Run the app (npm run dev) and open a product detail page where you have a Delivered order.
    2. Open the review form, attach 5 photos (confirm a 6th is rejected with the Turkish message), submit.
    3. Confirm the new review shows the "Doğrulanmış Alıcı" badge and a photo thumbnail row.
    4. Click a thumbnail — confirm the lightbox opens, prev/next works, Escape closes.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                              |
| ---------------------------------------------- | ---------------------------------------- |
| client -> Firebase Storage (uploadReviewPhoto) | Untrusted file content/size crosses here |
| photos[] -> POST /api/reviews                  | URL array crosses to the server          |

## STRIDE Threat Register

| Threat ID | Category          | Component                     | Disposition | Mitigation Plan                                                                                         |
| --------- | ----------------- | ----------------------------- | ----------- | ------------------------------------------------------------------------------------------------------- |
| T-07-06   | Denial of Service | photo count/size on upload    | mitigate    | Client caps at 5 and rejects non-image types; server Zod (07-01) caps photos at 5 and requires url()    |
| T-07-07   | Tampering         | forged photos[] sent directly | accept      | photos are display-only URLs under the review; server already bounds count; no privilege tied to photos |
| T-07-SC   | Tampering         | npm installs                  | mitigate    | No new package — lightbox uses plain React state. Any new install requires audit + blocking checkpoint  |

</threat_model>

<verification>
- npx tsc --noEmit passes for both tasks
- Human checkpoint confirms upload cap, gallery, and lightbox behavior
</verification>

<success_criteria>
Reviews can include up to 5 photos; photos display in a gallery with a lightbox on the product page.
</success_criteria>

<output>
Create `.planning/phases/07-reviews-trust/07-02-SUMMARY.md` when done.
</output>
