---
phase: 05-shipping-fulfillment
plan: 05
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - src/pages/SellerOrders.tsx
  - src/pages/OrderTracking.tsx
autonomous: false
requirements:
  - SHP-01
  - SHP-02
  - SHP-03

must_haves:
  truths:
    - "Seller sees 'Kargola + Etiket Al' button on Processing-status SubOrders"
    - 'Clicking the button calls POST /api/orders/:orderSetId/subOrders/:subOrderId/ship and shows trackingNumber + labelUrl on success'
    - 'Buyer order detail page shows trackingNumber, carrier, currentTrackingStatus, estimatedDelivery when SubOrder is shipped'
    - 'Tracking status displays with human-readable Turkish carrier status strings'
  artifacts:
    - path: 'src/pages/SellerOrders.tsx'
      provides: 'Updated handleShip calls backend route; displays returned trackingNumber + labelUrl'
      contains: '/api/orders'
    - path: 'src/pages/OrderTracking.tsx'
      provides: 'Tracking status section shows carrier, trackingNumber, currentTrackingStatus, estimatedDelivery'
      contains: 'currentTrackingStatus'
  key_links:
    - from: 'src/pages/SellerOrders.tsx'
      to: 'POST /api/orders/:orderSetId/subOrders/:subOrderId/ship'
      via: 'fetch or axios POST call'
      pattern: '/api/orders'
    - from: 'src/pages/OrderTracking.tsx'
      to: 'SubOrder Firestore fields'
      via: 'subOrder.currentTrackingStatus, subOrder.trackingNumber, subOrder.estimatedDelivery'
      pattern: 'currentTrackingStatus'
---

<objective>
Wire the carrier integration into the seller and buyer UI. Seller order page: replace the existing direct-Firestore handleShip with a call to the new POST .../ship backend route. Buyer order tracking page: display live tracking fields from SubOrder.

Purpose: After this plan, the end-to-end slice is complete — seller clicks "Kargola", backend creates shipment, SubOrder updates, buyer sees tracking status.
Output: Updated SellerOrders.tsx and OrderTracking.tsx.
</objective>

<execution_context>
@O:\AI\E-tic 2026\.planning\phases\05-shipping-fulfillment\05-PATTERNS.md
</execution_context>

<context>
@O:\AI\E-tic 2026\.planning\phases\05-shipping-fulfillment\05-CONTEXT.md

<interfaces>
<!-- Backend route contract (from Plan 03). -->

POST /api/orders/:orderSetId/subOrders/:subOrderId/ship
Request body: { weight?: number, desi?: number } (other fields read from seller profile + OrderSet server-side)
Response 200: { trackingNumber: string, labelUrl: string, estimatedDelivery: string }
Response 403: { error: 'forbidden' }
Response 500: { error: string }
Auth: Firebase ID token in Authorization: Bearer header

From src/types/order.ts (after Plan 01):
SubOrder.trackingNumber?: string
SubOrder.carrier?: string
SubOrder.currentTrackingStatus?: string
SubOrder.estimatedDelivery?: string
SubOrder.shippedAt?: string
SubOrder.deliveredAt?: string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SellerOrders.tsx — replace handleShip with backend route call + show label</name>
  <files>src/pages/SellerOrders.tsx</files>
  <read_first>
    - src/pages/SellerOrders.tsx — read fully; locate handleShip (lines 188-236); find the existing
      createCargoShipment + updateOrderStatus call; identify state variables (shipTarget, shipping, etc.);
      find the JSX "Kargo" button and the carrier selection UI; note existing imports (axios or fetch).
    - 05-PATTERNS.md §SellerOrders.tsx — existing handleShip pattern (lines 188-213) + Phase 5 change note
      (replace updateOrderStatus with backend route call).
    - 05-RESEARCH.md §D-03 decision — seller-initiated manual flow, server handles transition engine.
  </read_first>
  <action>
    Modify handleShip in src/pages/SellerOrders.tsx:
    1. Keep existing function signature and state variables (shipTarget, shipping, setShipping).
    2. Replace the body from createCargoShipment + updateOrderStatus pattern with:
       - Get Firebase ID token: const token = await firebaseUser.getIdToken()
       - POST to /api/orders/${shipTarget.orderSetId}/subOrders/${shipTarget.id}/ship
         with Authorization: Bearer ${token} header and JSON body { weight: weight || 1, desi: desi || 1 }
         where weight/desi come from existing form state if present, otherwise default to 1.
       - On 200: store result.trackingNumber + result.labelUrl + result.estimatedDelivery in component state
         (add useState<{trackingNumber:string;labelUrl:string;estimatedDelivery:string}|null>(null) named shipResult)
       - Show tracking number and a "Etiketi Görüntüle" link opening labelUrl in new tab after success
       - On non-200: surface error.message in existing error state variable
    3. Do NOT remove the carrier selector UI — it's still used for the request but the server routes per region.
    4. Button label: change to "Kargola + Etiket Al" (per D-03) if not already matching.
    5. Remove the now-unused import of createCargoShipment if no other usage exists in the file.
    6. Keep existing try/catch/finally pattern; setShipping(false) in finally.
    Note: SubOrder must have orderSetId in Firestore. Since Plan 01 added the field to the TypeScript type,
    confirm the existing shipTarget data structure has orderSetId (from the Firestore query); if the query
    only returns SubOrder docs without the parent OrderSet id, read the orderSetId from the SubOrder doc
    field (added in Plan 01) or from the existing query context.
  </action>
  <verify>
    <automated>cd "O:\AI\E-tic 2026" && npx tsc --noEmit 2>&1 | grep "SellerOrders" | head -10</automated>
  </verify>
  <done>
    - handleShip POSTs to /api/orders/:orderSetId/subOrders/:subOrderId/ship with bearer token
    - Success state shows trackingNumber and labelUrl link
    - Button label is "Kargola + Etiket Al"
    - TypeScript compiles without errors on this file
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 03 + Plan 05 Task 1 combined: backend shipment creation route + seller UI wired to it.
    Seller can click "Kargola + Etiket Al" → mock carrier returns tracking number → SubOrder transitions to shipped.
  </what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev (or tsx server.ts + vite dev in parallel)
    2. Log in as a seller account that has a Processing-status order
    3. Navigate to Seller Dashboard → Orders (or /seller/orders)
    4. Find an order in "Processing" status — the "Kargola + Etiket Al" button should be visible
    5. Click the button
    6. Expected: loading state appears, then trackingNumber (EN... or EZ... prefix) and "Etiketi Görüntüle" link appear
    7. Verify the order status changes to "Shipped" in the UI
    8. Check browser network tab: POST /api/orders/.../ship returns 200 with { trackingNumber, labelUrl, estimatedDelivery }
    9. Check Firebase console: subOrders/{id} document has trackingNumber, carrier, estimatedDelivery, status=shipped fields updated
  </how-to-verify>
  <resume-signal>Type "approved" if shipment creation works end-to-end, or describe the issue</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update OrderTracking.tsx — display live tracking fields from SubOrder</name>
  <files>src/pages/OrderTracking.tsx</files>
  <read_first>
    - src/pages/OrderTracking.tsx — read fully; find where SubOrder data is rendered; identify
      existing tracking display (trackingNumber, carrier); find the component's data fetch mechanism
      (Firestore onSnapshot or getDocs); note existing imports.
    - 05-PATTERNS.md §OrderTracking.tsx — import pattern + Phase 5 additions note.
    - src/types/order.ts (after Plan 01) — SubOrder interface with new fields.
  </read_first>
  <action>
    Extend src/pages/OrderTracking.tsx tracking display:
    1. In the SubOrder status section, add display of:
       - currentTrackingStatus: show as a status badge with Turkish label mapping:
         'pre_transit'→'Gönderime Hazırlanıyor', 'in_transit'→'Yolda', 'out_for_delivery'→'Dağıtımda',
         'delivered'→'Teslim Edildi', 'Kabul Edildi'→'Kabul Edildi', 'Yolda'→'Yolda',
         'Dağıtımda'→'Dağıtımda', 'Teslim Edildi'→'Teslim Edildi',
         unknown/undefined → 'Durum Bekleniyor'
       - estimatedDelivery: if present, show as "Tahmini Teslimat: {formatted date}" using
         toLocaleDateString('tr-TR')
       - trackingNumber: already likely displayed; ensure it shows when subOrder.trackingNumber is set
       - carrier: already likely displayed; ensure it shows
    2. Only render the tracking section when subOrder.status === 'shipped' || 'delivered'.
    3. For return request eligibility: if status === 'delivered' and deliveredAt is present:
       - Compute daysElapsed = (Date.now() - new Date(subOrder.deliveredAt).getTime()) / 86400000
       - If daysElapsed <= 14: show "İade Talebi Oluştur" button (href to return request form or
         onClick that navigates to the return section — the full return form UI is in Plan 06;
         here just show the disabled/active button depending on window)
       - If daysElapsed > 14: show "İade süresi doldu (14 gün)" text, no button
    4. Do NOT add the actual return request form/submission here — that's Plan 06.
    5. Keep all existing functionality intact.
  </action>
  <verify>
    <automated>cd "O:\AI\E-tic 2026" && npx tsc --noEmit 2>&1 | grep "OrderTracking" | head -10</automated>
  </verify>
  <done>
    - currentTrackingStatus displayed with Turkish label mapping when subOrder is shipped
    - estimatedDelivery shown as formatted date when present
    - "İade Talebi Oluştur" button visible for delivered orders within 14-day window
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                   |
| ------------------------------------------ | ------------------------------------------------------------- |
| Seller browser → POST /api/orders/.../ship | Bearer token sent; server validates ownership                 |
| Buyer browser → OrderTracking.tsx display  | Reads SubOrder from Firestore; Firestore rules control access |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                    | Disposition | Mitigation Plan                                                                                                                                        |
| ---------- | ---------------------- | -------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-05-05-01 | Information Disclosure | labelUrl displayed to buyer in OrderTracking | accept      | labelUrl in mock mode is a relative path to the same server; real mode will use Firebase Storage signed URLs; no sensitive carrier credentials exposed |
| T-05-05-02 | Tampering              | Client-side 14-day window check              | accept      | Client check is UX-only (disables button); server enforces authoritative window in Plan 04 returns route — client cannot bypass the 409 response       |
| T-05-05-03 | Spoofing               | Firebase ID token in Authorization header    | mitigate    | verifyFirebaseToken middleware on server validates token; getIdToken() on client ensures fresh token                                                   |
| T-05-SC    | Tampering              | npm installs                                 | accept      | No new packages; axios already in stack                                                                                                                |

</threat_model>

<verification>
1. npx tsc --noEmit — zero new errors on SellerOrders.tsx and OrderTracking.tsx
2. Manual checkpoint: seller clicks "Kargola + Etiket Al" → 200 response with trackingNumber (approved above)
3. In buyer order detail: navigate to a shipped order — currentTrackingStatus badge visible, estimatedDelivery date shown
4. For a delivered order within 14 days: "İade Talebi Oluştur" button present in DOM
</verification>

<success_criteria>

- SellerOrders.tsx: "Kargola + Etiket Al" calls POST backend route; shows trackingNumber + labelUrl on success
- OrderTracking.tsx: shipping status fields (trackingNumber, carrier, currentTrackingStatus, estimatedDelivery) all render when SubOrder is shipped
- Return window button shown/disabled based on deliveredAt + 14-day calculation
- Human checkpoint approved
  </success_criteria>

<output>
Create .planning/phases/05-shipping-fulfillment/05-05-SUMMARY.md when done
</output>
