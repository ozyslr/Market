---
phase: 05-shipping-fulfillment
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - server/services/__tests__/shipping.test.ts
  - server/services/__tests__/returns.test.ts
autonomous: true
requirements:
  - SHP-01
  - SHP-02
  - SHP-03
  - SHP-04
  - SHP-05

must_haves:
  truths:
    - 'Failing tests exist for MockEntegiProvider, MockEasyPostProvider, routeCarrierByRegion'
    - 'Failing tests exist for return window enforcement (>14 days rejected) and processRefund wire-up'
    - 'Test files follow the refundService.test.ts static import + vi.mock pattern'
  artifacts:
    - path: 'server/services/__tests__/shipping.test.ts'
      provides: 'RED tests for SHP-01/02/03/04'
      contains: 'MockEntegiProvider'
    - path: 'server/services/__tests__/returns.test.ts'
      provides: 'RED tests for SHP-05'
      contains: 'return_window_expired'
  key_links:
    - from: 'server/services/__tests__/shipping.test.ts'
      to: 'src/services/cargoService.ts'
      via: 'static import createCargoShipment, routeCarrierByRegion'
      pattern: 'routeCarrierByRegion'
---

<objective>
Create RED (failing) test scaffolds for all Phase 5 behaviors before implementation. This plan establishes the TDD gate: tests must fail now, then pass after Plans 03/04 implement the routes and logic.

Purpose: Nyquist rule — all new behaviors need automated verification commands. Tests for shipping provider behavior, region routing, cron poll delivery transition, 14-day return window enforcement, and processRefund wire-up.
Output: Two test files with failing tests. Wave 2 route plans (03, 04) make them pass.
</objective>

<execution_context>
@O:\AI\E-tic 2026\.planning\phases\05-shipping-fulfillment\05-PATTERNS.md
</execution_context>

<context>
@O:\AI\E-tic 2026\.planning\phases\05-shipping-fulfillment\05-RESEARCH.md
</context>

<feature>
  <name>Shipping provider + returns behavior tests (RED phase)</name>
  <files>server/services/__tests__/shipping.test.ts, server/services/__tests__/returns.test.ts</files>
  <behavior>
    shipping.test.ts covers:
    - MockEntegiProvider.createShipment returns object with success=true, trackingNumber starting 'EN', labelUrl string
    - MockEasyPostProvider.createShipment returns object with success=true, trackingNumber starting 'EZ', labelUrl string
    - routeCarrierByRegion('TR') === 'Entegi'
    - routeCarrierByRegion('DE') === 'EasyPost'
    - routeCarrierByRegion('FR') === 'EasyPost'
    - getTrackingStatus for Entegi returns object with success=true, trackingNumber, provider='Entegi'
    - getTrackingStatus for EasyPost returns object with success=true, trackingNumber, provider='EasyPost'

    returns.test.ts covers:
    - Return request with deliveredAt=15 days ago is rejected with error code 'return_window_expired'
    - Return request with deliveredAt=3 days ago is accepted (within window)
    - Return approval calls processRefund with correct { orderSetId, subOrderId, isFullRefund: true }

  </behavior>
  <implementation>
    Copy file structure from the analog: server/services/__tests__/refundService.test.ts
    (static imports at top, vi.mock for Firestore adminDb, describe/it/expect pattern).
    Tests in shipping.test.ts import from src/services/cargoService.ts using .js extension (ESM).
    Tests in returns.test.ts mock the return window validation logic inline (no route import needed —
    test the business rule function extracted or inline the date math test).
    Both files must be syntactically valid TS even in RED state — the tests fail at assertion, not at parse.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Create shipping.test.ts — RED tests for SHP-01/02/03/04</name>
  <files>server/services/__tests__/shipping.test.ts</files>
  <read_first>
    - server/services/__tests__/refundService.test.ts — read fully; extract: static import pattern
      at top, vi.mock usage, describe/it/expect structure, beforeEach shape.
    - 05-PATTERNS.md §shipping.test.ts — test file structure pattern with import paths and describe blocks.
    - 05-RESEARCH.md §Validation Architecture §Phase Requirements Test Map — exact test cases for SHP-01..SHP-04.
  </read_first>
  <action>
    Create server/services/__tests__/shipping.test.ts with:
    1. Static imports: import { describe, it, expect } from 'vitest';
       import { createCargoShipment, getTrackingStatus, routeCarrierByRegion } from '../../../src/services/cargoService.js';
    2. describe('routeCarrierByRegion', ...) — three it() blocks: TR→Entegi, DE→EasyPost, FR→EasyPost.
       These will PASS immediately after Plan 01 (routeCarrierByRegion is pure, no async).
    3. describe('MockEntegiProvider', ...) — two it() blocks:
       - createShipment: call createCargoShipment('Entegi', minimalReq) where minimalReq has
         receiverCountry='TR' and required fields; expect result.success=true,
         result.trackingNumber to match /^EN/i, typeof result.labelUrl === 'string'.
       - getTracking: call getTrackingStatus('Entegi', 'EN12345678') expect result.success=true,
         result.provider==='Entegi'.
    4. describe('MockEasyPostProvider', ...) — two it() blocks matching above but for 'EasyPost'
       and /^EZ/i trackingNumber pattern.
    All tests are async. minimalReq object uses literal values (no faker dependency).
    File must parse/compile cleanly even before Plan 01 is complete (imports will resolve once Plan 01 ships).
  </action>
  <verify>
    <automated>cd "O:\AI\E-tic 2026" && npx vitest run server/services/__tests__/shipping.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - File exists at server/services/__tests__/shipping.test.ts
    - 7 test cases present (3 routing + 2 Entegi + 2 EasyPost)
    - Tests run (pass or fail with assertion errors, not parse errors)
    - After Plan 01 completes: all 7 pass
  </done>
</task>

<task type="tdd">
  <name>Task 2: Create returns.test.ts — RED tests for SHP-05 window + refund wire-up</name>
  <files>server/services/__tests__/returns.test.ts</files>
  <read_first>
    - server/services/__tests__/refundService.test.ts — read fully; extract vi.mock pattern for
      adminDb and processRefund mocking approach.
    - 05-PATTERNS.md §returns.ts — ReturnRequest interface shape (fields available for test data).
    - 05-RESEARCH.md §Pitfall 3 (14-day window enforcement) — exact server-side guard logic to test.
    - 05-PATTERNS.md §returns.ts §14-day window server-side guard pattern — WINDOW_MS calculation.
  </read_first>
  <action>
    Create server/services/__tests__/returns.test.ts with:
    1. Static imports: import { describe, it, expect, vi } from 'vitest';
       Import the return window validation helper — if it doesn't exist yet, test the inline logic:
         function isReturnWindowExpired(deliveredAt: string): boolean {
           const WINDOW_MS = 14 * 24 * 60 * 60 * 1000;
           return Date.now() - new Date(deliveredAt).getTime() > WINDOW_MS;
         }
       This inline helper tests the exact business rule that will be extracted into the route.
    2. describe('return window enforcement', ...) — two it() blocks:
       - 'rejects request older than 14 days': deliveredAt = new Date(Date.now() - 15*24*60*60*1000).toISOString();
         expect(isReturnWindowExpired(deliveredAt)).toBe(true)
       - 'accepts request within 14 days': deliveredAt = new Date(Date.now() - 3*24*60*60*1000).toISOString();
         expect(isReturnWindowExpired(deliveredAt)).toBe(false)
    3. describe('return approval processRefund wire-up', ...) — one it() block:
       vi.mock the processRefund import; call the approval handler logic (inline or via spy);
       verify processRefund was called with { orderSetId: 'set-1', subOrderId: 'sub-1', isFullRefund: true }.
       If the route doesn't exist yet (Plan 04 not done), mark as: it.todo('approval calls processRefund with isFullRefund:true')
    Tests use only vi, no external services. No real Firestore calls.
  </action>
  <verify>
    <automated>cd "O:\AI\E-tic 2026" && npx vitest run server/services/__tests__/returns.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - File exists at server/services/__tests__/returns.test.ts
    - 3 test cases present (2 window + 1 processRefund or todo)
    - All window tests pass immediately (pure date math)
    - processRefund test passes after Plan 04 completes
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                 |
| ------------------------------- | --------------------------------------------------------------------------- |
| test files → production imports | Tests import from src/services — no trust boundary concern; test-only scope |

## STRIDE Threat Register

| Threat ID  | Category  | Component                    | Disposition | Mitigation Plan                                                           |
| ---------- | --------- | ---------------------------- | ----------- | ------------------------------------------------------------------------- |
| T-05-02-01 | Tampering | Test data in returns.test.ts | accept      | Tests run in Vitest sandbox; no production data touched                   |
| T-05-SC    | Tampering | npm/pip/cargo installs       | accept      | No new packages installed in this plan; Vitest already in devDependencies |

</threat_model>

<verification>
1. npx vitest run server/services/__tests__/shipping.test.ts — exits with test results (even if some fail)
2. npx vitest run server/services/__tests__/returns.test.ts — window tests pass; processRefund test is todo or passes
3. grep -c "routeCarrierByRegion\|MockEntegiProvider\|MockEasyPostProvider" server/services/__tests__/shipping.test.ts returns >= 3
</verification>

<success_criteria>

- Both test files exist and parse without TypeScript errors
- shipping.test.ts: 7 test cases covering routing (3) + Entegi (2) + EasyPost (2)
- returns.test.ts: window enforcement tests pass; processRefund test present (todo or passing)
- Full suite npx vitest run is not broken by these new files
  </success_criteria>

<output>
Create .planning/phases/05-shipping-fulfillment/05-02-SUMMARY.md when done
</output>
