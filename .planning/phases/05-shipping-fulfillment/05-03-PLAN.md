---
phase: 05-shipping-fulfillment
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - server/routes/shipping.ts
  - server/routes/carrierWebhook.ts
  - server/routes/carrierPoll.ts
  - server/services/emailService.ts
  - server.ts
autonomous: true
requirements:
  - SHP-01
  - SHP-02
  - SHP-03
  - SHP-04

must_haves:
  truths:
    - 'POST /api/orders/:orderSetId/subOrders/:subOrderId/ship creates shipment, writes trackingNumber+carrier+estimatedDelivery to SubOrder, transitions status to shipped'
    - 'EasyPost webhook endpoint is registered before express.json() in server.ts; HMAC verified via crypto.timingSafeEqual when EASYPOST_WEBHOOK_SECRET is set'
    - 'POST /api/carrier/easypost/webhook deduplicates on event.id via processedWebhooks collection'
    - 'POST /api/carrier/entegi/poll transitions SubOrders with delivered tracking to Delivered state and fires delivery email'
    - 'POST /api/carrier/check-delays finds overdue SubOrders and fires sendDelayNotificationEmail'
    - 'sendDelayNotificationEmail exists in emailService.ts'
  artifacts:
    - path: 'server/routes/shipping.ts'
      provides: 'registerShippingRoutes — POST .../ship endpoint'
      exports: ['registerShippingRoutes']
    - path: 'server/routes/carrierWebhook.ts'
      provides: 'registerCarrierWebhook — raw-body EasyPost webhook'
      exports: ['registerCarrierWebhook']
    - path: 'server/routes/carrierPoll.ts'
      provides: 'registerCarrierPollRoutes — Entegi poll + delay check'
      exports: ['registerCarrierPollRoutes']
    - path: 'server/services/emailService.ts'
      provides: 'sendDelayNotificationEmail added'
      contains: 'sendDelayNotificationEmail'
  key_links:
    - from: 'server/routes/shipping.ts'
      to: 'src/services/cargoService.ts'
      via: 'routeCarrierByRegion + createCargoShipment'
      pattern: 'routeCarrierByRegion'
    - from: 'server/routes/carrierWebhook.ts'
      to: 'processedWebhooks'
      via: "adminDb.collection('processedWebhooks').doc(eventId)"
      pattern: 'processedWebhooks'
    - from: 'server/routes/carrierPoll.ts'
      to: 'transitionEngine'
      via: "transitionSubOrder(adminDb, sub.orderSetId, doc.id, 'confirm_delivery')"
      pattern: 'confirm_delivery'
---

<objective>
Build the three backend route modules (shipping creation, EasyPost webhook, Entegi cron poll + delay check) and extend emailService with the delay notification function. Register all routes in server.ts. This plan delivers the complete server-side shipping slice: seller triggers label creation → carrier mock responds → SubOrder updated → tracking poll confirms delivery → delay check fires notifications.

Purpose: After this plan, POST /api/orders/.../ship is callable by the seller UI (Plan 05 depends on it).
Output: 3 new route files, 1 extended service, server.ts wired.
</objective>

<execution_context>
@O:\AI\E-tic 2026\.planning\phases\05-shipping-fulfillment\05-PATTERNS.md
</execution_context>

<context>
@O:\AI\E-tic 2026\.planning\ROADMAP.md
@O:\AI\E-tic 2026\.planning\phases\05-shipping-fulfillment\05-CONTEXT.md
@O:\AI\E-tic 2026\.planning\phases\05-shipping-fulfillment\05-RESEARCH.md

<interfaces>
<!-- Contracts available after Plan 01 ships. Executor should read source files directly. -->

From src/services/cargoService.ts (after Plan 01):
export function routeCarrierByRegion(country: string): CargoProviderName
export async function createCargoShipment(provider: CargoProviderName, req: ShipmentRequest): Promise<ShipmentResponse>
export async function getTrackingStatus(provider: CargoProviderName, trackingNumber: string): Promise<TrackingResponse>

From server/services/emailService.ts (existing):
export async function sendDeliveryConfirmationEmail(orderSetId, customerEmail, customerName, carrier): Promise<void>
export async function sendShippingUpdateEmail(orderSetId, customerEmail, customerName, trackingNumber, carrier): Promise<void>

From server.ts (existing registration order pattern lines 295-300):
registerStripeWebhook(app, stripe, adminDb); // raw body, before express.json()
app.use(express.json());
// JSON routes registered after this line

From server/services/transitionEngine.ts (Phase 2):
export async function transitionSubOrder(adminDb, orderSetId, subOrderId, event, payload?): Promise<void>
Valid events include: 'mark_shipped', 'confirm_delivery'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shipping.ts route + add sendDelayNotificationEmail to emailService.ts</name>
  <files>server/routes/shipping.ts, server/services/emailService.ts</files>
  <read_first>
    - server/routes/orders.ts — read fully; extract: DI deps interface pattern (lines 40-58),
      verifyFirebaseToken guard usage, try/catch/res.status(500).json({error}) shape,
      Firestore doc fetch pattern (adminDb.collection().doc().get()).
    - server/routes/stripe.ts lines 44-64 — auth guard pattern for seller ownership check.
    - server/services/emailService.ts — read fully; extract: sendDeliveryConfirmationEmail signature
      (lines 136-158), logger.error non-blocking pattern, APP_URL reference, sendEmail call shape.
    - 05-PATTERNS.md §shipping.ts — DI deps interface + registerShippingRoutes skeleton excerpt.
    - 05-PATTERNS.md §emailService.ts — sendDelayNotificationEmail full pattern excerpt.
    - 05-RESEARCH.md §Architecture Patterns §System Architecture Diagram — shipment creation flow steps 1-6.
  </read_first>
  <behavior>
    shipping.ts:
    - POST /api/orders/:orderSetId/subOrders/:subOrderId/ship requires verifyFirebaseToken + verifySeller
    - Load SubOrder doc from adminDb.collection('subOrders').doc(subOrderId).get()
    - Verify subOrder.sellerId === req.uid (seller ownership); return 403 if mismatch
    - Read shippingAddress from parent OrderSet (adminDb.collection('orderSets').doc(orderSetId).get())
    - routeCarrierByRegion(shippingAddress.country || 'TR') to get providerName
    - Build ShipmentRequest with: orderId=subOrderId, receiverCountry=shippingAddress.country,
      receiverName/address/city/phone from OrderSet.shippingAddress,
      senderName/address/city/phone from seller profile (adminDb.collection('sellers').doc(req.uid).get())
    - createCargoShipment(providerName, shipmentReq) → ShipmentResponse
    - On success: adminDb.collection('subOrders').doc(subOrderId).update({
        trackingNumber: result.trackingNumber, carrier: providerName,
        estimatedDelivery: result.estimatedDelivery, labelCost: result.labelCost ?? null,
        currentTrackingStatus: 'label_created' })
    - transitionSubOrder(adminDb, orderSetId, subOrderId, 'mark_shipped', { version: subOrder.version })
    - void sendShippingUpdateEmail(...) non-blocking
    - Return res.json({ trackingNumber, labelUrl, estimatedDelivery })
    emailService.ts:
    - sendDelayNotificationEmail(orderSetId, customerEmail, customerName, estimatedDelivery, carrier): Promise<void>
    - Copy try/catch + logger.error pattern from sendDeliveryConfirmationEmail
    - subject: 'Siparişiniz Gecikmeli — #${orderSetId.slice(0,8).toUpperCase()}'
    - HTML template inline or via delayNotificationHtml helper — copy structure from existing template helpers
  </behavior>
  <action>
    Create server/routes/shipping.ts:
    1. Define interface ShippingRouteDeps with: adminDb: Firestore | null; verifyFirebaseToken: Middleware; verifySeller: Middleware.
    2. Export registerShippingRoutes(app: Express, deps: ShippingRouteDeps): void.
    3. Implement POST /api/orders/:orderSetId/subOrders/:subOrderId/ship with the flow above.
    4. Seller ownership: subOrder.sellerId !== req.uid → res.status(403).json({ error: 'forbidden' }).
    5. Use try/catch; on error res.status(err.statusCode ?? 500).json({ error: err.message }).
    Extend server/services/emailService.ts:
    6. Add sendDelayNotificationEmail export after sendDeliveryConfirmationEmail block.
       Parameters: orderSetId: string, customerEmail: string, customerName: string,
       estimatedDelivery: string, carrier: string.
    7. Construct HTML string inline (no separate template file needed — copy the pattern from
       the nearest existing simple template in emailService.ts; include customerName, carrier,
       estimatedDelivery formatted as locale date string, link to /orders/${orderSetId}).
    8. Call sendEmail({ to: customerEmail, subject: ..., html }) inside try/catch; on catch logger.error non-blocking.
    Do NOT modify any existing emailService functions. Do NOT break existing route imports.
  </action>
  <verify>
    <automated>cd "O:\AI\E-tic 2026" && npx tsc --noEmit 2>&1 | grep -E "shipping\.ts|emailService" | head -20</automated>
  </verify>
  <done>
    - server/routes/shipping.ts exports registerShippingRoutes
    - Ownership guard (sellerId !== uid → 403) present in code
    - sendDelayNotificationEmail exported from emailService.ts
    - TypeScript compile clean on both files
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create carrierWebhook.ts + carrierPoll.ts + wire all routes in server.ts</name>
  <files>server/routes/carrierWebhook.ts, server/routes/carrierPoll.ts, server.ts</files>
  <read_first>
    - server/routes/stripe.ts lines 44-64 — registerStripeWebhook: express.raw({type:'application/json'}),
      raw body before JSON, exact registration pattern. CRITICAL analog for carrierWebhook.ts.
    - server/routes/payouts.ts lines 14-22 — verifyCronSecret endpoint pattern. Analog for carrierPoll.ts.
    - server/routes/iyzico.ts lines 188-207 — two-phase processedWebhooks dedup pattern
      (pre-check get + runTransaction re-check). Analog for EasyPost webhook dedup.
    - server.ts lines 285-310 — find the exact lines where registerStripeWebhook is called and
      where express.json() is called; note line numbers for insertion points.
    - 05-PATTERNS.md §carrierWebhook.ts — registerCarrierWebhook raw-body pattern + server.ts registration.
    - 05-PATTERNS.md §carrierPoll.ts — registerCarrierPollRoutes with Entegi poll + delay check endpoints.
    - 05-PATTERNS.md §Shared Patterns §Webhook Dedup — two-phase dedup pattern excerpt.
    - 05-RESEARCH.md §HMAC Verification — validateEasyPostHmac function using crypto.timingSafeEqual.
  </read_first>
  <behavior>
    carrierWebhook.ts:
    - Exports registerCarrierWebhook(app, deps: { adminDb }): void
    - Route: POST /api/carrier/easypost/webhook with express.raw({ type: 'application/json' }) — raw body
    - validateEasyPostHmac(secret, signature, body): boolean using crypto.createHmac('sha256',secret).update(body).digest('hex') + crypto.timingSafeEqual; returns false if lengths differ
    - Skip HMAC check if EASYPOST_WEBHOOK_SECRET env var is empty/absent (mock mode)
    - Dedup: adminDb.collection('processedWebhooks').doc(event.id) pre-get; if exists return 200
    - Inside runTransaction: re-check dedup doc; if not exists: set dedup marker, process event
    - If event.description === 'tracker.updated' and result.status === 'delivered': transitionSubOrder(..., 'confirm_delivery') + void sendDeliveryConfirmationEmail(...)
    - Else: update SubOrder.currentTrackingStatus = result.status
    - Return res.status(200).json({ received: true })

    carrierPoll.ts:
    - Exports registerCarrierPollRoutes(app, deps: { adminDb, verifyCronSecret }): void
    - POST /api/carrier/entegi/poll protected by verifyCronSecret middleware:
      Query subOrders where carrier=='Entegi', status=='shipped', shippedAt >= 30 days ago
      For each: getTrackingStatus('Entegi', sub.trackingNumber) → if delivered: transitionSubOrder + void sendDeliveryConfirmationEmail; else update currentTrackingStatus
      Return res.json({ polled: N, results })
    - POST /api/carrier/check-delays protected by verifyCronSecret:
      Query subOrders where status=='shipped', estimatedDelivery < now ISO string, no deliveredAt field
      For each: fetch OrderSet for customerEmail/name; void sendDelayNotificationEmail(...)
      Return res.json({ notified: N })

    server.ts wiring (CRITICAL ORDER):
    - Import registerCarrierWebhook from ./server/routes/carrierWebhook
    - Import registerShippingRoutes from ./server/routes/shipping
    - Import registerCarrierPollRoutes from ./server/routes/carrierPoll
    - registerCarrierWebhook(app, { adminDb }) AFTER registerStripeWebhook and BEFORE app.use(express.json())
    - registerShippingRoutes and registerCarrierPollRoutes AFTER app.use(express.json())

  </behavior>
  <action>
    Create server/routes/carrierWebhook.ts:
    1. Import express, crypto, firebase-admin/firestore FieldValue, transitionSubOrder, sendDeliveryConfirmationEmail.
    2. Inline function validateEasyPostHmac(secret: string, signature: string, body: Buffer): boolean — crypto.createHmac + timingSafeEqual; guard against length mismatch before timingSafeEqual call.
    3. Export registerCarrierWebhook with the raw-body route and two-phase dedup pattern.
    4. On tracker.updated+delivered: call transitionSubOrder(adminDb, sub.orderSetId, sub.id, 'confirm_delivery') — note: need to query subOrders by trackingNumber to find orderSetId.

    Create server/routes/carrierPoll.ts:
    5. Import getTrackingStatus, sendDeliveryConfirmationEmail, sendDelayNotificationEmail, transitionSubOrder.
    6. Export registerCarrierPollRoutes with both cron endpoints.
    7. Entegi poll query: adminDb.collection('subOrders').where('carrier','==','Entegi').where('status','==','shipped').where('shippedAt','>=', thirtyDaysAgo).get() — thirtyDaysAgo is new Date(Date.now()-30*86400*1000).toISOString().
    8. Delay check query: adminDb.collection('subOrders').where('status','==','shipped').get() then filter in-memory on estimatedDelivery < now && !deliveredAt (Firestore cannot do < on string + missing field combo cleanly — in-memory filter is safe for current scale).

    Edit server.ts:
    9. Add imports for the three new route registration functions after existing route imports.
    10. Add registerCarrierWebhook(app, { adminDb }) immediately after the registerStripeWebhook call and before app.use(express.json()).
    11. Add registerShippingRoutes and registerCarrierPollRoutes calls after the existing JSON-body route registrations.
    Do NOT change the express.json() middleware line or its position relative to Stripe webhook.

  </action>
  <verify>
    <automated>cd "O:\AI\E-tic 2026" && npx tsc --noEmit 2>&1 | grep -E "carrierWebhook|carrierPoll|server\.ts" | head -20</automated>
  </verify>
  <done>
    - server/routes/carrierWebhook.ts exports registerCarrierWebhook; contains validateEasyPostHmac with timingSafeEqual
    - server/routes/carrierPoll.ts exports registerCarrierPollRoutes; two cron endpoints present
    - server.ts: registerCarrierWebhook called before express.json(); registerShippingRoutes and registerCarrierPollRoutes called after
    - npx tsc --noEmit passes with no new errors
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                              |
| --------------------------------------------- | -------------------------------------------------------- |
| Internet → POST /api/carrier/easypost/webhook | Unauthenticated inbound POST; must verify HMAC signature |
| Cron service → POST /api/carrier/entegi/poll  | External trigger; must verify CRON_SECRET                |
| Cron service → POST /api/carrier/check-delays | External trigger; must verify CRON_SECRET                |
| Seller browser → POST /api/orders/.../ship    | Authenticated; must verify seller owns the SubOrder      |

## STRIDE Threat Register

| Threat ID  | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                                    |
| ---------- | ---------------------- | ---------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-03-01 | Spoofing               | POST /api/carrier/easypost/webhook | mitigate    | validateEasyPostHmac with X-Hmac-Signature header + crypto.timingSafeEqual; skip only when EASYPOST_WEBHOOK_SECRET is empty (mock mode explicitly) |
| T-05-03-02 | Tampering              | EasyPost webhook replay            | mitigate    | processedWebhooks dedup on event.id via two-phase transaction (pre-check + inside-transaction re-check; reuses Phase 2 02-02 pattern)              |
| T-05-03-03 | Elevation of Privilege | POST .../ship seller ownership     | mitigate    | subOrder.sellerId !== req.uid → 403; verifyFirebaseToken + verifySeller middleware chain                                                           |
| T-05-03-04 | Elevation of Privilege | Cron endpoints without auth        | mitigate    | verifyCronSecret middleware on /entegi/poll and /check-delays (same as payout/abandoned-cart crons)                                                |
| T-05-03-05 | Information Disclosure | labelUrl in SubOrder Firestore doc | accept      | Mock URLs are non-sensitive relative paths; real label PDFs stored in Firebase Storage with signed URLs in Phase real-key activation               |
| T-05-SC    | Tampering              | npm installs                       | accept      | No new npm packages installed; @easypost/api deferred per D-01                                                                                     |

</threat_model>

<verification>
1. npx tsc --noEmit — zero new TypeScript errors
2. grep "registerCarrierWebhook" server.ts — appears BEFORE the "express.json()" line
3. grep -c "timingSafeEqual" server/routes/carrierWebhook.ts — returns >= 1
4. grep -c "processedWebhooks" server/routes/carrierWebhook.ts — returns >= 1
5. grep -c "verifyCronSecret" server/routes/carrierPoll.ts — returns >= 2 (one per endpoint)
6. curl -X POST http://localhost:3000/api/carrier/entegi/poll (without CRON_SECRET) — returns 401/403
</verification>

<success_criteria>

- POST /api/orders/:orderSetId/subOrders/:subOrderId/ship protected, creates shipment, writes tracking fields, transitions to shipped
- EasyPost webhook registered before express.json(); HMAC verified; dedup via processedWebhooks
- Entegi poll and delay-check cron endpoints protected by verifyCronSecret
- sendDelayNotificationEmail available in emailService.ts
- Zero new TypeScript compile errors
  </success_criteria>

<output>
Create .planning/phases/05-shipping-fulfillment/05-03-SUMMARY.md when done
</output>
