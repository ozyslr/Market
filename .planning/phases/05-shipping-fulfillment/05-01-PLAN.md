---
phase: 05-shipping-fulfillment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/cargoService.ts
  - src/types/order.ts
  - src/types/returns.ts
autonomous: true
requirements:
  - SHP-01
  - SHP-02

must_haves:
  truths:
    - "routeCarrierByRegion('TR') returns 'Entegi'; routeCarrierByRegion('DE') returns 'EasyPost'"
    - 'MockEntegiProvider.createShipment returns trackingNumber starting EN + labelUrl as relative path'
    - 'MockEasyPostProvider.createShipment returns trackingNumber starting EZ + labelUrl as relative path'
    - 'SubOrder interface includes orderSetId, currentTrackingStatus, estimatedDelivery, labelCost'
    - 'ReturnRequest interface exists in src/types/returns.ts with all required fields'
  artifacts:
    - path: 'src/services/cargoService.ts'
      provides: 'MockEntegiProvider, MockEasyPostProvider, routeCarrierByRegion, extended types'
      contains: 'CargoProviderName.*Entegi.*EasyPost'
    - path: 'src/types/order.ts'
      provides: 'SubOrder with orderSetId back-reference and tracking cache fields'
      contains: 'orderSetId'
    - path: 'src/types/returns.ts'
      provides: 'ReturnRequest, ReturnStatus, ReturnReason types'
      exports: ['ReturnRequest', 'ReturnStatus', 'ReturnReason']
  key_links:
    - from: 'server/routes/carrierPoll.ts'
      to: 'src/services/cargoService.ts'
      via: 'routeCarrierByRegion + getTrackingStatus'
      pattern: 'routeCarrierByRegion'
    - from: 'server/routes/shipping.ts'
      to: 'src/services/cargoService.ts'
      via: 'createCargoShipment(providerName, shipmentReq)'
      pattern: 'createCargoShipment'
---

<objective>
Extend the carrier abstraction (cargoService.ts) with Entegi and EasyPost mock providers, fix the four code gaps identified in RESEARCH.md, create the ReturnRequest type contract, and add the region-routing function. This plan delivers the type + service foundation that all downstream plans (routes, UI) depend on.

Purpose: Without these contracts in place, Plans 03-06 have no stable types to import. Interface-first ordering per the planner's task ordering rule.
Output: Extended cargoService.ts, updated order.ts, new returns.ts — all consumed by Wave 2 route plans.
</objective>

<execution_context>
@O:\AI\E-tic 2026\.planning\phases\05-shipping-fulfillment\05-PATTERNS.md
</execution_context>

<context>
@O:\AI\E-tic 2026\.planning\ROADMAP.md
@O:\AI\E-tic 2026\.planning\phases\05-shipping-fulfillment\05-CONTEXT.md
@O:\AI\E-tic 2026\.planning\phases\05-shipping-fulfillment\05-RESEARCH.md

<interfaces>
<!-- Key existing contracts. Executor reads these files directly per read_first. -->

From src/services/cargoService.ts (current CargoProviderName union — line 11):
export type CargoProviderName = 'PTT' | 'Yurtici' | 'Aras' | 'MNG' | 'Surat' | 'UPS' | 'DHL';

From src/services/cargoService.ts (ShipmentRequest — lines 13-29):
interface ShipmentRequest { orderId, senderName, senderAddress, senderCity, senderPhone,
receiverName, receiverAddress, receiverCity, receiverPhone, weight, desi, ...}

From src/types/order.ts (SubOrder — lines 56-72):
interface SubOrder { id, sellerId, items, subtotal, shippingCost, commission, payoutAmount,
status, trackingNumber?, carrier?, shippedAt?, deliveredAt?, version, createdAt, updatedAt }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend cargoService.ts — add Entegi/EasyPost providers + region routing</name>
  <files>src/services/cargoService.ts</files>
  <read_first>
    - src/services/cargoService.ts — read entire file; extract: CargoProviderName union (line 11),
      ShipmentRequest/ShipmentResponse/TrackingResponse interfaces (lines 13-50),
      existing MockPttProvider class body (lines 119-167 pattern to copy),
      providerRegistry block (lines 263-278 where new registrations go),
      delay() helper location.
    - 05-PATTERNS.md §cargoService.ts — type union extension + MockEntegiProvider full body +
      MockEasyPostProvider full body + routeCarrierByRegion + provider registry extension excerpts.
    - 05-RESEARCH.md §Pattern 1 (region routing) + §Pattern 5 (EasyPost mock) + §Pattern 6 (Entegi mock).
  </read_first>
  <behavior>
    - CargoProviderName union includes 'Entegi' and 'EasyPost'
    - ShipmentRequest has receiverCountry: string (required), isReturn?: boolean, labelCost?: number
    - ShipmentResponse has labelCost?: number
    - MockEntegiProvider implements CargoProvider: createShipment returns trackingNumber 'EN'+8-char random uppercase + labelUrl '/api/cargo/label/${tracking}' + estimatedDelivery ISO + shippingCost 30-50 TRY range
    - MockEasyPostProvider implements CargoProvider: createShipment returns trackingNumber 'EZ'+10-char random uppercase + labelUrl '/api/cargo/label/${tracking}' + estimatedDelivery ISO + shippingCost 9-25 EUR range
    - Both providers' getTracking returns delivered=true when currentStatus matches provider's delivered string
    - routeCarrierByRegion('TR') === 'Entegi'; routeCarrierByRegion(anything else) === 'EasyPost'
    - providerRegistry.set registers both new providers
    - No real API calls; no import of @easypost/api (deferred per D-01)
  </behavior>
  <action>
    Read cargoService.ts fully. Then make targeted edits in this order:
    1. Extend CargoProviderName union on line 11: append | 'Entegi' | 'EasyPost' (per D-01, D-02b).
    2. Add three fields to ShipmentRequest: receiverCountry: string; isReturn?: boolean; labelCost?: number.
    3. Add one field to ShipmentResponse: labelCost?: number.
    4. Add MockEntegiProvider class after the last existing mock provider class, implementing CargoProvider with name = 'Entegi'. Copy the MockPttProvider class structure. labelUrl must use relative path '/api/cargo/label/${tracking}' (not https://mock-entegi.com — avoids pitfall 5 from RESEARCH.md). getTracking delivered sentinel: statuses[idx] === 'Teslim Edildi'.
    5. Add MockEasyPostProvider class after MockEntegiProvider. name = 'EasyPost'. trackingNumber prefix 'EZ'. Same relative labelUrl pattern. EasyPost status enum: 'pre_transit' | 'in_transit' | 'out_for_delivery' | 'delivered'; delivered sentinel: currentStatus === 'delivered'.
    6. Inside getProviders() or the providerRegistry initialization block, add: providerRegistry.set('Entegi', new MockEntegiProvider()); providerRegistry.set('EasyPost', new MockEasyPostProvider());
    7. After the registry block, export: export function routeCarrierByRegion(country: string): CargoProviderName { return country === 'TR' ? 'Entegi' : 'EasyPost'; }
    Do NOT add any import for @easypost/api. Do NOT remove existing providers. Do NOT add fenced code blocks inside action.
  </action>
  <verify>
    <automated>cd "O:\AI\E-tic 2026" && npx tsc --noEmit 2>&1 | grep -i "cargoService\|returns\|order" | head -20</automated>
  </verify>
  <done>
    - CargoProviderName includes 'Entegi' and 'EasyPost'
    - ShipmentRequest has receiverCountry, isReturn?, labelCost? fields
    - MockEntegiProvider and MockEasyPostProvider classes present and registered
    - routeCarrierByRegion exported, returns 'Entegi' for 'TR' and 'EasyPost' otherwise
    - TypeScript compiles without errors on this file
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend order.ts (SubOrder gaps) + create returns.ts type contract</name>
  <files>src/types/order.ts, src/types/returns.ts</files>
  <read_first>
    - src/types/order.ts — read entire file; locate SubOrder interface (lines 56-72); confirm which
      fields already exist among: orderSetId, currentTrackingStatus, estimatedDelivery, labelCost.
    - 05-PATTERNS.md §order.ts — SubOrder additions excerpt (four fields with types).
    - 05-PATTERNS.md §returns.ts — ReturnStatus union + ReturnReason union + ReturnRequest interface
      full excerpt including windowExpiresAt.
    - 05-RESEARCH.md §Pattern 4 (returns data model) — confirms separate collection rationale.
  </read_first>
  <action>
    Step A — order.ts SubOrder gaps (per RESEARCH.md §Existing Code Gaps):
    Add to SubOrder interface only the fields that are NOT already present:
      orderSetId: string          (required, back-reference for cron poll per RESEARCH Pitfall 6)
      currentTrackingStatus?: string   (polling result cache)
      estimatedDelivery?: string       (ISO string from ShipmentResponse)
      labelCost?: number               (label billing readiness)
    Do NOT remove or reorder existing fields. Do NOT change any other interface.

    Step B — returns.ts (new file, src/types/returns.ts):
    Create file with three named exports following the union-type + interface pattern from order.ts:
      export type ReturnStatus = 'pending' | 'approved' | 'rejected' | 'label_sent' | 'received' | 'refunded';
      export type ReturnReason = 'wrong_item' | 'damaged' | 'not_as_described' | 'changed_mind' | 'other';
      export interface ReturnRequest fields:
        id: string; orderSetId: string; subOrderId: string; buyerId: string; sellerId: string;
        reason: ReturnReason; notes?: string; status: ReturnStatus;
        returnTrackingNumber?: string; returnLabelUrl?: string; approvedBy?: string;
        rejectionReason?: string; refundId?: string; ledgerEntryIds?: string[];
        createdAt: string; updatedAt: string; windowExpiresAt: string;
    Use 'export interface' and 'export type' (named exports, not default exports — per project convention).

  </action>
  <verify>
    <automated>cd "O:\AI\E-tic 2026" && npx tsc --noEmit 2>&1 | grep -E "order\.ts|returns\.ts" | head -10</automated>
  </verify>
  <done>
    - SubOrder interface has orderSetId: string field
    - src/types/returns.ts exists with ReturnStatus, ReturnReason, ReturnRequest exports
    - ReturnRequest.windowExpiresAt: string present
    - TypeScript compiles without errors on both files
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                     |
| --------------------------------- | ------------------------------------------------------------------------------- |
| service layer → provider registry | Provider lookup by name string; if unregistered name passed, getProvider throws |

## STRIDE Threat Register

| Threat ID | Category  | Component                                 | Disposition | Mitigation Plan                                                                                                                      |
| --------- | --------- | ----------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| T-05-01   | Spoofing  | MockEntegiProvider / MockEasyPostProvider | accept      | Mock providers are server-side only; no API keys; no network calls in mock mode                                                      |
| T-05-02   | Tampering | routeCarrierByRegion                      | accept      | Pure function on country code string; output is an enum member; no external input reaches it directly without route-layer validation |
| T-05-SC   | Tampering | npm installs                              | accept      | No new npm installs in this plan; @easypost/api intentionally deferred per D-01                                                      |

</threat_model>

<verification>
1. npx tsc --noEmit passes with no errors in src/services/cargoService.ts, src/types/order.ts, src/types/returns.ts
2. grep -c "Entegi\|EasyPost" src/services/cargoService.ts returns >= 6 (union + both classes + registry + routing fn)
3. grep -c "orderSetId" src/types/order.ts returns >= 1
4. File src/types/returns.ts exists and grep -c "windowExpiresAt" returns >= 1
</verification>

<success_criteria>

- CargoProviderName type includes 'Entegi' and 'EasyPost'
- MockEntegiProvider and MockEasyPostProvider registered in providerRegistry
- routeCarrierByRegion exported pure function
- SubOrder.orderSetId exists (fixes refundService.ts line 88 dependency)
- ReturnRequest type contract available for all downstream plans
- Zero TypeScript compile errors on modified files
  </success_criteria>

<output>
Create .planning/phases/05-shipping-fulfillment/05-01-SUMMARY.md when done
</output>
