---
phase: 04-search-discovery
plan: 04
type: execute
wave: 3
depends_on: ['04-03']
files_modified:
  - src/components/commerce/FilterPanel.tsx
  - src/pages/SearchResults.tsx
autonomous: true
requirements: [SRC-01, SRC-02, SRC-03, SRC-04]

must_haves:
  truths:
    - "Sort dropdown shows 'En Çok Satan / Best Selling' as the second option (after newest, before price options)"
    - 'normalizeTR is no longer called before the search query in SearchResults.tsx'
    - 'Searching from the UI returns Typesense results (typo-tolerant)'
    - 'Facet counts from Typesense appear beside brand names in the filter panel'
    - 'Skeleton loading shows for a minimum of 150ms during Typesense queries'
  artifacts:
    - path: 'src/components/commerce/FilterPanel.tsx'
      provides: 'SORT_OPTIONS array with best-selling entry at position 2'
      contains: 'best-selling'
    - path: 'src/pages/SearchResults.tsx'
      provides: 'normalizeTR removed from search call path; artificial delay reduced to 150ms minimum'
      contains: '150'
  key_links:
    - from: 'src/pages/SearchResults.tsx'
      to: 'src/services/searchService.ts'
      via: 'searchProducts(params) — params must not have normalizeTR applied to query'
      pattern: 'searchProducts'
    - from: 'src/components/commerce/FilterPanel.tsx'
      to: 'SearchParams.sortBy'
      via: "SORT_OPTIONS value 'best-selling' maps to sort_by:'salesCount:desc' in searchService"
      pattern: 'best-selling'
---

<objective>
Complete the UI side of the search engine swap: add the "best-selling" sort option to FilterPanel, remove the normalizeTR call from SearchResults.tsx search path, and reduce the artificial skeleton delay to the 150ms minimum specified in UI-SPEC.

Purpose: After this plan, buyers see the complete sort control (SRC-03), typo-tolerant results flow correctly (D-09), and the skeleton does not flash unnecessarily on fast Typesense responses.
Output: FilterPanel.tsx with best-selling sort; SearchResults.tsx with normalizeTR removed from query path and 150ms minimum skeleton.
</objective>

<execution_context>
@O:/AI/E-tic 2026/.planning/../node_modules/.bin/../../../.claude/get-shit-done/workflows/execute-plan.md
@O:/AI/E-tic 2026/.planning/../node_modules/.bin/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@O:/AI/E-tic 2026/.planning/ROADMAP.md
@O:/AI/E-tic 2026/.planning/phases/04-search-discovery/04-CONTEXT.md
@O:/AI/E-tic 2026/.planning/phases/04-search-discovery/04-UI-SPEC.md
@O:/AI/E-tic 2026/.planning/phases/04-search-discovery/04-PATTERNS.md
@O:/AI/E-tic 2026/.planning/phases/04-search-discovery/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add best-selling sort option to FilterPanel.tsx</name>
  <files>src/components/commerce/FilterPanel.tsx</files>
  <read_first>
    - src/components/commerce/FilterPanel.tsx (MUST read full file — find the SORT_OPTIONS array or equivalent; find the t() / LanguageContext usage for sort option labels; confirm current sort option values and their order)
    - .planning/phases/04-search-discovery/04-UI-SPEC.md (§1 Sort control — exact position and TR/EN/DE/AR label copy)
    - .planning/phases/04-search-discovery/04-PATTERNS.md (FilterPanel.tsx section — t() pattern for labels)
  </read_first>
  <action>
    Locate the SORT_OPTIONS array (or its equivalent constant/array) in FilterPanel.tsx. It currently contains entries for newest, price-asc, price-desc, and rating.

    Insert a new entry at index 1 (second position, after newest/default, before price options):
    { value: 'best-selling', label: t('sort.bestSelling') ?? 'En Çok Satan' }

    If the component uses a translation key lookup pattern (t('sort.xxx') or similar from LanguageContext), follow the same pattern as the existing entries. If the existing entries use hardcoded strings instead of t(), use the same approach for consistency.

    Final SORT_OPTIONS order per UI-SPEC §1:
    1. newest (value '' or 'newest') — default
    2. best-selling (value 'best-selling') — NEW
    3. price_asc
    4. price_desc
    5. rating

    Do NOT change any other part of FilterPanel.tsx. Do NOT add a new visual element — this is only an option in the existing sort select dropdown. Do NOT change styling.

    If the component uses i18n key lookup, also ensure the key 'sort.bestSelling' resolves in at least TR ('En Çok Satan') and EN ('Best Selling'). If translation keys are defined inline in the component (not in a separate i18n file), add the key to the same inline structure. If translations are in a separate file (e.g. src/i18n/ or a translations object in LanguageContext), add the key there — check where existing sort labels are defined before deciding.

  </action>
  <verify>
    <automated>grep -c "best-selling" "O:/AI/E-tic 2026/src/components/commerce/FilterPanel.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - FilterPanel.tsx contains 'best-selling' as a sort option value (grep returns >= 1)
    - The entry appears at index 1 in the sort options array (after the default/newest entry)
    - The label uses the same translation lookup pattern as existing sort options
    - No other styling or layout changes are present in FilterPanel.tsx
    - tsc --noEmit passes for this file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove normalizeTR from search path and tune skeleton delay in SearchResults.tsx</name>
  <files>src/pages/SearchResults.tsx</files>
  <read_first>
    - src/pages/SearchResults.tsx (MUST read full file — find the normalizeTR call site on the query before searchProducts(); find the artificial setTimeout or delay that controls skeleton display; find SearchResultsSkeleton usage)
    - .planning/phases/04-search-discovery/04-PATTERNS.md (SearchResults.tsx section — normalizeTR removal instructions)
    - .planning/phases/04-search-discovery/04-UI-SPEC.md (§4 Loading/skeleton state — 150ms minimum, no visual change to skeleton)
    - .planning/phases/04-search-discovery/04-CONTEXT.md (D-09 decision — drop normalizeTR from Typesense search path only; keep function itself)
  </read_first>
  <action>
    Change 1 — Remove normalizeTR from search call path:
    Find the call site where normalizeTR is applied to the search query parameter before passing it to searchProducts() (or before constructing the SearchParams object that goes to searchProducts). Remove only that specific normalizeTR() call/wrapping on the query string. Do NOT delete the normalizeTR import if it is used elsewhere in SearchResults.tsx (e.g. for category matching). If normalizeTR is only used at that one search-path call site, remove the import too.

    Change 2 — Tune skeleton delay:
    Find any artificial setTimeout, delay, or minimum display timer that is set to a value above 150ms for the search skeleton. If the current artificial delay is > 150ms (e.g. 300ms), reduce it to 150ms. If no artificial delay exists, add a minimum display guard of 150ms so the skeleton does not flash on fast Typesense responses: track searchStartTime = Date.now() when search begins; after results arrive, setTimeout for Math.max(0, 150 - (Date.now() - searchStartTime)) before hiding the skeleton.

    Do NOT change any visual aspects of SearchResultsSkeleton. Do NOT change any layout, color, spacing, or component structure. These are the ONLY two changes permitted in this task.

  </action>
  <verify>
    <automated>grep -c "normalizeTR" "O:/AI/E-tic 2026/src/pages/SearchResults.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - normalizeTR is no longer called on the query string before searchProducts() in SearchResults.tsx
    - If normalizeTR was only used at that call site, the import is also removed (grep "normalizeTR" SearchResults.tsx returns 0; otherwise >= 1 for remaining usages)
    - Skeleton minimum display time is 150ms (grep "150" SearchResults.tsx returns >= 1 in the delay/timer logic)
    - No layout, color, spacing, or component structure changes
    - tsc --noEmit passes for this file
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Trigger admin reindex and verify end-to-end search</name>
  <read_first>
    - .planning/phases/04-search-discovery/04-02-SUMMARY.md (confirm reindex endpoint is registered)
    - .planning/phases/04-search-discovery/04-03-SUMMARY.md (confirm searchService Typesense path is live)
  </read_first>
  <what-built>
    All search infrastructure is now deployed: server routes, client service, sync hooks, UI updates.
    This checkpoint verifies the full end-to-end flow works against the live Typesense Cloud cluster.
  </what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev (or tsx server.ts)
    2. Obtain an admin Firebase ID token (log in as admin, use browser devtools to get the token from Firebase Auth).
    3. Trigger initial backfill — run in terminal:
       curl -X POST http://localhost:3000/api/admin/reindex \
         -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
         -H "Content-Type: application/json"
       Expected response: {"indexed":N} where N >= 0 (0 is ok if no products in Firestore yet).
    4. Navigate to the Search page in the browser (e.g. http://localhost:5173/search?q=test).
    5. Type a search query. Confirm results appear (or empty state if no matching products).
    6. Type a typo query (e.g. "telofon" if phone products exist, or try a 1-char typo of any product title). Confirm Typesense returns results anyway.
    7. Apply a category filter — confirm the facet count updates alongside results.
    8. Change sort to "En Çok Satan" — confirm the dropdown shows the new option.
    9. Check browser devtools Network tab — search requests should go to Typesense Cloud (*.typesense.net), not to /api/.
    10. Create or update a product as a seller (via Seller Dashboard or API). Within 5 seconds, repeat the search — confirm the new/updated product appears in results.
  </how-to-verify>
  <resume-signal>Type "verified" if all checks pass, or describe which step failed</resume-signal>
  <acceptance_criteria>
    - POST /api/admin/reindex returns {"indexed":N} with no error
    - Typesense search results appear in the browser search page
    - Typo-tolerant query returns results (does not return empty for a 1-char typo of an existing product title)
    - "En Çok Satan" appears in the sort dropdown at position 2
    - Product create/update triggers sync and product appears in search results within ~5 seconds
    - Browser Network tab shows search requests going directly to Typesense Cloud (not proxied through Express)
    - Firestore fallback still works: if VITE_TYPESENSE_SEARCH_API_KEY is temporarily set to an invalid value, search degrades to Firestore/MOCK results without an error page
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                           |
| ------------------------------- | ----------------------------------------------------- |
| browser → Typesense Cloud       | Read-only search-only key; acceptable per D-02        |
| admin → POST /api/admin/reindex | Only called manually; verifyAdmin enforces role check |

## STRIDE Threat Register

| Threat ID | Category               | Component                      | Disposition | Mitigation Plan                                                                                                                                            |
| --------- | ---------------------- | ------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-12   | Information Disclosure | normalizeTR removal            | accept      | Removing normalizeTR from search path simplifies code; no security implication — the normalization was a query pre-processing step, not a security control |
| T-04-13   | Denial of Service      | 150ms minimum skeleton delay   | accept      | Client-side timer; no server impact; improves UX without security risk                                                                                     |
| T-04-14   | Elevation of Privilege | reindex triggered by non-admin | mitigate    | verifyAdmin enforced on /api/admin/reindex in Plan 02 (not modified here); this plan only calls the endpoint manually for verification                     |

</threat_model>

<verification>
After all three tasks:
1. tsc --noEmit — zero errors across entire project
2. grep -c "best-selling" src/components/commerce/FilterPanel.tsx — returns >= 1
3. grep -c "normalizeTR" src/pages/SearchResults.tsx — returns 0 (if only used at search call site) or review remaining usages are not in search path
4. npm run build — clean build with no TypeScript errors
5. End-to-end search working per Task 3 acceptance criteria
</verification>

<success_criteria>

- FilterPanel.tsx has 'best-selling' sort option at position 2 (after newest)
- SearchResults.tsx does not apply normalizeTR to Typesense query path
- Skeleton has 150ms minimum display time
- Full end-to-end search works: Typesense returns typo-tolerant results, facets update, sort options work
- Phase 4 SRC-01 through SRC-05 all verifiably complete
  </success_criteria>

<output>
Create .planning/phases/04-search-discovery/04-04-SUMMARY.md when done
</output>
