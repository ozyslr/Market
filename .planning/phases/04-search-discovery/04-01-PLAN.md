---
phase: 04-search-discovery
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .env
  - .env.example
autonomous: false
requirements: [SRC-01, SRC-02, SRC-03, SRC-04, SRC-05]

must_haves:
  truths:
    - 'Four Typesense env vars are present in .env and documented in .env.example'
    - 'Typesense Cloud cluster is reachable from the dev machine'
  artifacts:
    - path: '.env'
      provides: 'TYPESENSE_HOST, TYPESENSE_ADMIN_API_KEY, VITE_TYPESENSE_HOST, VITE_TYPESENSE_SEARCH_API_KEY'
      contains: 'TYPESENSE_HOST'
    - path: '.env.example'
      provides: 'Documentation of required Typesense env vars'
      contains: 'TYPESENSE_HOST'
  key_links:
    - from: 'server/lib/typesenseAdmin.ts'
      to: 'Typesense Cloud'
      via: 'process.env.TYPESENSE_HOST + process.env.TYPESENSE_ADMIN_API_KEY'
      pattern: 'TYPESENSE_ADMIN_API_KEY'
    - from: 'src/lib/typesense.ts'
      to: 'Typesense Cloud'
      via: 'import.meta.env.VITE_TYPESENSE_SEARCH_API_KEY'
      pattern: 'VITE_TYPESENSE_SEARCH_API_KEY'
---

<objective>
Provision the Typesense Cloud cluster and record the four required environment variables before any search code is written or tested.

Purpose: All Wave 1+ plans depend on a live Typesense Cloud endpoint. Without the four env vars, the typesenseAdmin and typesenseClient initializations will throw at startup.
Output: Four env vars in .env; corresponding placeholder lines in .env.example.
</objective>

<execution_context>
@O:/AI/E-tic 2026/.planning/../node_modules/.bin/../../../.claude/get-shit-done/workflows/execute-plan.md
@O:/AI/E-tic 2026/.planning/../node_modules/.bin/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@O:/AI/E-tic 2026/.planning/PROJECT.md
@O:/AI/E-tic 2026/.planning/ROADMAP.md
@O:/AI/E-tic 2026/.planning/phases/04-search-discovery/04-CONTEXT.md
@O:/AI/E-tic 2026/.planning/phases/04-search-discovery/04-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision Typesense Cloud cluster and collect env vars</name>
  <read_first>
    - .planning/phases/04-search-discovery/04-CONTEXT.md (D-01, D-02 decisions)
    - .planning/phases/04-search-discovery/04-RESEARCH.md (Environment Availability section)
    - .env.example (existing env var format)
  </read_first>
  <what-built>Nothing yet — this task collects the four secrets required by all subsequent plans.</what-built>
  <how-to-verify>
    1. Go to https://cloud.typesense.org and sign in (create account if needed).
    2. Create a new cluster — choose the closest region (Frankfurt for EU, or Istanbul if available).
    3. Once the cluster is "Running", copy the hostname shown (e.g. xyz.a1.typesense.net).
    4. In the cluster dashboard, go to API Keys. Note the default Admin API Key.
    5. Create a new API key scoped to "documents:search" on collection "products" — this is the search-only key (safe to ship to the browser per D-02).
    6. Collect all four values:
       - TYPESENSE_HOST = the cluster hostname (no https://, no port)
       - TYPESENSE_ADMIN_API_KEY = the admin key (server-side only — never commit to source)
       - VITE_TYPESENSE_HOST = same hostname as TYPESENSE_HOST (Vite exposes to browser)
       - VITE_TYPESENSE_SEARCH_API_KEY = the search-only scoped key
    7. Add all four to .env (create if it does not exist). Format: KEY=value, one per line.
    8. Verify cluster connectivity: curl -H "X-TYPESENSE-API-KEY: <ADMIN_KEY>" "https://<HOST>/health" should return {"ok":true}.
  </how-to-verify>
  <resume-signal>Type "provisioned" when all four env vars are in .env and the health check returns ok:true</resume-signal>
  <acceptance_criteria>
    - .env contains TYPESENSE_HOST, TYPESENSE_ADMIN_API_KEY, VITE_TYPESENSE_HOST, VITE_TYPESENSE_SEARCH_API_KEY
    - curl https://$TYPESENSE_HOST/health returns {"ok":true}
    - TYPESENSE_ADMIN_API_KEY value is NOT equal to VITE_TYPESENSE_SEARCH_API_KEY value (they are distinct keys)
    - VITE_TYPESENSE_SEARCH_API_KEY is scoped to documents:search only (verify in Typesense Cloud key list)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Document env vars in .env.example</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (current contents — must append without breaking existing entries)
  </read_first>
  <action>
    Append a "Typesense Search" section to .env.example with four placeholder lines:
    TYPESENSE_HOST=your-cluster.a1.typesense.net
    TYPESENSE_ADMIN_API_KEY=your-admin-key-here
    VITE_TYPESENSE_HOST=your-cluster.a1.typesense.net
    VITE_TYPESENSE_SEARCH_API_KEY=your-search-only-key-here

    Add a comment above the section: "# Typesense Search (Phase 4) — provision at cloud.typesense.org"
    Add a note: "# VITE_TYPESENSE_SEARCH_API_KEY must be a search-only scoped key (safe for browser). TYPESENSE_ADMIN_API_KEY is server-side only."
    Do NOT add actual key values. Do NOT modify any existing lines.

  </action>
  <verify>
    <automated>grep -c "TYPESENSE_HOST" "O:/AI/E-tic 2026/.env.example"</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains TYPESENSE_HOST, TYPESENSE_ADMIN_API_KEY, VITE_TYPESENSE_HOST, VITE_TYPESENSE_SEARCH_API_KEY as placeholder lines
    - Comment explains which key is browser-safe and which is server-only
    - No real key values appear in .env.example
    - grep -c "TYPESENSE_HOST" .env.example returns 2 (one TYPESENSE_HOST, one VITE_TYPESENSE_HOST)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                   |
| ----------------------------- | ----------------------------------------------------------------------------- |
| .env → process                | Admin key loaded from .env into process.env — never shipped to browser bundle |
| .env.example → source control | Only placeholder strings, never real keys                                     |

## STRIDE Threat Register

| Threat ID | Category               | Component                     | Disposition | Mitigation Plan                                                                                  |
| --------- | ---------------------- | ----------------------------- | ----------- | ------------------------------------------------------------------------------------------------ |
| T-04-01   | Information Disclosure | .env file                     | mitigate    | .env is in .gitignore (verify); admin key never appears in VITE\_ prefixed var                   |
| T-04-02   | Elevation of Privilege | VITE_TYPESENSE_SEARCH_API_KEY | accept      | Key is intentionally browser-visible per D-02; scoped to documents:search only — no write access |
| T-04-SC   | Tampering              | npm/pip/cargo installs        | mitigate    | typesense package is [ASSUMED] — human-verify checkpoint in Plan 02 before install               |

</threat_model>

<verification>
After both tasks:
1. Run: grep "TYPESENSE" "O:/AI/E-tic 2026/.env.example" — should show 4 lines
2. Confirm .env is listed in .gitignore: grep ".env" "O:/AI/E-tic 2026/.gitignore"
3. Confirm no key values appear in .env.example: grep -v "your-" "O:/AI/E-tic 2026/.env.example" | grep "TYPESENSE" should return only comment lines
</verification>

<success_criteria>

- Four Typesense env vars present in .env with real values
- Four placeholder lines documented in .env.example with explanatory comments
- Cluster health endpoint returns {"ok":true}
- .env is gitignored (admin key never reaches source control)
  </success_criteria>

<output>
Create .planning/phases/04-search-discovery/04-01-SUMMARY.md when done
</output>
