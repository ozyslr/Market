---
phase: 04-search-discovery
plan: 02
type: execute
wave: 1
depends_on: ['04-01']
files_modified:
  - server/lib/typesenseAdmin.ts
  - server/lib/typesenseSchema.ts
  - server/lib/typesenseSynonyms.ts
  - server/routes/search.ts
  - server.ts
autonomous: false
requirements: [SRC-01, SRC-05]

must_haves:
  truths:
    - 'POST /api/search/sync upserts or deletes a Typesense document; returns {ok:true}'
    - 'POST /api/admin/reindex bulk-imports all Firestore products; returns {indexed:N}'
    - 'typesense npm package is installed and verified'
    - 'A curated TR/EN synonym set (incl. telefon/phone/cep telefonu) is upserted into the products collection (D-09)'
    - 'D-03 satisfied: sync is server write-through via an authenticated Express endpoint (no Cloud Functions / polling)'
    - 'D-04 satisfied: POST /api/search/sync verifies Firebase token + seller ownership, then upserts/deletes in Typesense with the admin key'
    - 'D-05 satisfied: admin-only POST /api/admin/reindex bulk-imports all Firestore products (backfill + drift recovery)'
    - 'D-08 satisfied: a single products collection schema carries multi-language title fields'
  artifacts:
    - path: 'server/lib/typesenseAdmin.ts'
      provides: 'Server-side Typesense admin client singleton'
      exports: ['typesenseAdmin']
    - path: 'server/lib/typesenseSchema.ts'
      provides: 'PRODUCTS_SCHEMA constant + toTypesenseDoc() mapping helper'
      exports: ['PRODUCTS_SCHEMA', 'toTypesenseDoc']
    - path: 'server/routes/search.ts'
      provides: 'registerSearchRoutes(app, deps) — POST /api/search/sync + POST /api/admin/reindex'
      exports: ['registerSearchRoutes']
    - path: 'server/lib/typesenseSynonyms.ts'
      provides: 'PRODUCT_SYNONYMS curated set + upsertProductSynonyms() helper (D-09)'
      exports: ['PRODUCT_SYNONYMS', 'upsertProductSynonyms']
  key_links:
    - from: 'server.ts'
      to: 'server/routes/search.ts'
      via: 'registerSearchRoutes(app, { adminDb, verifyFirebaseToken, verifyAdmin })'
      pattern: 'registerSearchRoutes'
    - from: 'server/routes/search.ts'
      to: 'typesenseAdmin'
      via: "typesenseAdmin.collections('products').documents().upsert()"
      pattern: 'typesenseAdmin'
---

<objective>
**Decisions covered:** D-03 (server write-through sync), D-04 (POST /api/search/sync after client Firestore write, token + ownership verified), D-05 (admin-only POST /api/admin/reindex backfill/drift-recovery), D-08 (single products collection with multi-language title fields in PRODUCTS_SCHEMA), D-09 (TR/EN synonym set upserted during reindex). D-02 admin key stays server-side only.

Install the typesense npm package and build the complete server-side search infrastructure: admin client, schema/mapping helper, and the two Express endpoints (sync + reindex).

Purpose: Establishes the write path — products can now be upserted/deleted in Typesense from authenticated server code, and existing Firestore products can be bulk-indexed via admin reindex. This is the foundation for SRC-05 (event-driven updates).
Output: typesense installed; server/lib/typesenseAdmin.ts, server/lib/typesenseSchema.ts, server/routes/search.ts created; server.ts updated to register routes.
</objective>

<execution_context>
@O:/AI/E-tic 2026/.planning/../node_modules/.bin/../../../.claude/get-shit-done/workflows/execute-plan.md
@O:/AI/E-tic 2026/.planning/../node_modules/.bin/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@O:/AI/E-tic 2026/.planning/ROADMAP.md
@O:/AI/E-tic 2026/.planning/phases/04-search-discovery/04-CONTEXT.md
@O:/AI/E-tic 2026/.planning/phases/04-search-discovery/04-RESEARCH.md
@O:/AI/E-tic 2026/.planning/phases/04-search-discovery/04-PATTERNS.md
@O:/AI/E-tic 2026/.planning/phases/04-search-discovery/04-01-SUMMARY.md

<interfaces>
From server/routes/commission.ts (DI pattern to mirror):
```typescript
export function registerCommissionRoutes(app: Express, deps: CommissionDeps): void
// deps contains: adminDb, verifyFirebaseToken, verifyAdmin
```

From src/lib/authMiddleware.ts:

```typescript
// verifyFirebaseToken: attaches req.uid, req.decodedToken to request
// verifyAdmin: calls verifyFirebaseToken then checks decoded.role === 'admin'
```

From server.ts (registration call site pattern):

```typescript
registerSearchRoutes(app, { adminDb, verifyFirebaseToken, verifyAdmin });
```

</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify typesense npm package before install</name>
  <read_first>
    - .planning/phases/04-search-discovery/04-RESEARCH.md (Package Legitimacy Audit section — package is [ASSUMED])
  </read_first>
  <what-built>Nothing yet — this verifies the package legitimacy before installation.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/typesense in a browser.
    2. Confirm: publisher is "typesense" org, weekly downloads > 100k, package age > 2 years.
    3. Visit https://github.com/typesense/typesense-js — confirm it is the official Typesense client repo.
    4. Run: npm view typesense version — confirm a version >= 1.8.0 is returned.
  </how-to-verify>
  <resume-signal>Type "verified" if the package checks out, or describe any concerns</resume-signal>
  <acceptance_criteria>
    - npmjs.com/package/typesense shows publisher "typesense", downloads > 100k/week
    - GitHub repo github.com/typesense/typesense-js exists and is maintained by Typesense Inc.
    - npm view typesense version returns a semver string >= 1.8.0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install typesense package and create server lib files</name>
  <files>
    server/lib/typesenseAdmin.ts,
    server/lib/typesenseSchema.ts,
    package.json
  </files>
  <read_first>
    - server/lib/ (list existing files to confirm directory exists and naming convention)
    - .planning/phases/04-search-discovery/04-PATTERNS.md (typesenseAdmin.ts and typesenseSchema.ts pattern sections)
    - .planning/phases/04-search-discovery/04-RESEARCH.md (Pattern 1: Typesense Collection Schema, toTypesenseDoc() code example)
    - src/types.ts (Product interface — to confirm field names for toTypesenseDoc mapping)
  </read_first>
  <action>
    Step 1 — Install package: run "npm install typesense" from the project root. Do not pin a specific version unless npm install resolves a version older than 1.7.0 (in which case add @1.8.x).

    Step 2 — Create server/lib/typesenseAdmin.ts:
    - Named export: typesenseAdmin = new Typesense.Client with nodes[0] using process.env.TYPESENSE_HOST (port 443, protocol 'https'), apiKey: process.env.TYPESENSE_ADMIN_API_KEY!, connectionTimeoutSeconds: 10.
    - No default export. Mirror pattern from PATTERNS.md typesenseAdmin section exactly.
    - Use .js extension on all local imports (ESM requirement per PATTERNS.md Shared Patterns: ESM Import Extensions).

    Step 3 — Create server/lib/typesenseSchema.ts:
    - Export named const PRODUCTS_SCHEMA with the full field list from PATTERNS.md (all 21 fields including id, sellerId, sellerName, title, title_tr/en/de/ar optional, description, brand facet:true, categoryId facet:true, price float, rating float facet:true, reviewsCount int32, salesCount int32, stock int32, freeShipping bool facet:true, status, images string[], tags string[] optional, createdAtTs int64). default_sorting_field: 'salesCount'.
    - Export function toTypesenseDoc(product: Record&lt;string,any&gt;, id: string, sellerName = ''): Record&lt;string,any&gt; — handles createdAt as Firestore Timestamp (toMillis()), number, string, or missing (falls back to Date.now()). salesCount defaults to product.reviewsCount ?? 0 when absent (proxy for MVP per RESEARCH.md Pitfall 1). All fields nullable-safe with ?? defaults.
    - Import Timestamp type from 'firebase-admin/firestore' (type-only import).

  </action>
  <verify>
    <automated>node -e "import('./server/lib/typesenseAdmin.js').then(m => console.log(typeof m.typesenseAdmin))" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains "typesense" with a version >= 1.7.0
    - server/lib/typesenseAdmin.ts exports named const typesenseAdmin (Typesense.Client instance)
    - server/lib/typesenseSchema.ts exports PRODUCTS_SCHEMA (object with name:'products' and 21 fields) and toTypesenseDoc function
    - PRODUCTS_SCHEMA.default_sorting_field === 'salesCount'
    - toTypesenseDoc handles Timestamp.toMillis(), string date, number date, and undefined createdAt without throwing
    - All local imports use .js extension
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create server/routes/search.ts and register in server.ts</name>
  <files>
    server/routes/search.ts,
    server.ts
  </files>
  <read_first>
    - server/routes/search.ts (does not exist yet — creating fresh)
    - server.ts (must read before editing — find existing registerXxxRoutes call pattern and the deps object shape)
    - server/routes/commission.ts (DI pattern analog per PATTERNS.md)
    - src/lib/authMiddleware.ts (verifyFirebaseToken and verifyAdmin signatures)
    - .planning/phases/04-search-discovery/04-PATTERNS.md (server/routes/search.ts full pattern section including auth+ownership check and admin reindex patterns)
  </read_first>
  <action>
    Create server/routes/search.ts:

    Export function registerSearchRoutes(app: Express, deps: SearchRouteDeps): void.
    SearchRouteDeps interface: { adminDb: Firestore; verifyFirebaseToken: middleware; verifyAdmin: middleware }.
    Import typesenseAdmin from '../lib/typesenseAdmin.js', toTypesenseDoc and PRODUCTS_SCHEMA from '../lib/typesenseSchema.js'.
    All local imports use .js extension.

    POST /api/search/sync (verifyFirebaseToken middleware):
    - Validate req.body.productId (non-empty string) and req.body.action (must be 'upsert' or 'delete'). Return 400 if invalid.
    - Read product from adminDb.collection('products').doc(productId). Return 404 if not found.
    - Ownership check: product.sellerId !== req.uid AND req.decodedToken?.role !== 'admin' → return 403 with error 'Bu ürün size ait değil'.
    - If action === 'delete': typesenseAdmin.collections('products').documents(productId).delete().
    - If action === 'upsert': call toTypesenseDoc(product.data(), productId, product.data().sellerName ?? '') then typesenseAdmin.collections('products').documents().upsert(doc).
    - Wrap Typesense call in try/catch → return res.status(500).json({ error: err.message }) on failure.
    - Success: return res.json({ ok: true }).

    POST /api/admin/reindex (verifyAdmin middleware):
    - Ensure collection exists: try typesenseAdmin.collections('products').retrieve(); catch → create with PRODUCTS_SCHEMA as any.
    - Build seller name map: single pass over adminDb.collection('sellers').get(), Map&lt;string,string&gt; sellerId→storeName.
    - Fetch all products: adminDb.collection('products').get(). Map each doc to toTypesenseDoc(d.data(), d.id, sellerNames.get(d.data().sellerId) ?? '').
    - Bulk import: typesenseAdmin.collections('products').documents().import(docs, { action: 'upsert' }).
    - Return res.json({ indexed: docs.length }) on success, res.status(500).json({ error: err.message }) on failure.

    Error response format: mirror sellerApi.ts pattern exactly (see PATTERNS.md Error Response Format).

    Update server.ts:
    - Add import: import { registerSearchRoutes } from './server/routes/search.js';
    - Add registration call alongside other registerXxxRoutes calls (after the last existing registerXxxRoutes call, before the Vite/static catch-all): registerSearchRoutes(app, { adminDb, verifyFirebaseToken, verifyAdmin });
    - Do not change any other server.ts logic.

  </action>
  <verify>
    <automated>grep -c "registerSearchRoutes" "O:/AI/E-tic 2026/server.ts"</automated>
  </verify>
  <acceptance_criteria>
    - server/routes/search.ts exports registerSearchRoutes with correct DI signature
    - server.ts contains "import { registerSearchRoutes } from './server/routes/search.js'" and "registerSearchRoutes(app," — grep -c "registerSearchRoutes" server.ts returns 2
    - POST /api/search/sync with invalid action returns 400 (verifiable by inspecting the condition in the source)
    - POST /api/search/sync ownership check uses req.uid (not req.user.uid — match the actual authMiddleware field name from authMiddleware.ts)
    - POST /api/admin/reindex uses verifyAdmin middleware (not verifyFirebaseToken)
    - All imports use .js extension for local server files
    - tsc --noEmit passes with no new errors in server/routes/search.ts or server/lib/ files
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Create curated TR/EN synonym set and upsert into the products collection (D-09)</name>
  <files>
    server/lib/typesenseSynonyms.ts,
    server/routes/search.ts
  </files>
  <read_first>
    - server/lib/typesenseSynonyms.ts (does not exist yet — creating fresh)
    - server/lib/typesenseAdmin.ts (typesenseAdmin client created in Task 2 — import target)
    - server/routes/search.ts (reindex handler created in Task 3 — must read before editing)
    - .planning/phases/04-search-discovery/04-CONTEXT.md (D-09 locked decision — TR/EN synonym set)
    - .planning/phases/04-search-discovery/04-RESEARCH.md (Typesense synonyms API: multi-way synonym sets)
  </read_first>
  <action>
    Step 1 — Create server/lib/typesenseSynonyms.ts:
    - Named export const PRODUCT_SYNONYMS: array of multi-way synonym sets. Minimum curated set MUST include: ['telefon', 'phone', 'cep telefonu', 'smartphone'], ['laptop', 'dizüstü', 'notebook'], ['kulaklık', 'headphone', 'earphone'], ['televizyon', 'tv', 'televizyon']. Each entry shaped { id: string; synonyms: string[] } where id is a stable slug (e.g. 'syn-telefon').
    - Named export async function upsertProductSynonyms(): for each entry, call typesenseAdmin.collections('products').synonyms().upsert(entry.id, { synonyms: entry.synonyms }). Wrap each in try/catch, console.warn on failure, continue (fire-and-forward; a single bad synonym must not abort the batch). Return the count successfully upserted.
    - Import typesenseAdmin from './typesenseAdmin.js' (ESM .js extension).

    Step 2 — Wire into the reindex flow in server/routes/search.ts:
    - Import upsertProductSynonyms from '../lib/typesenseSynonyms.js'.
    - Inside POST /api/admin/reindex, AFTER the collection-exists/create step and AFTER the bulk import succeeds, call await upsertProductSynonyms().
    - Include synonyms count in the response: res.json({ indexed: docs.length, synonyms: synCount }).

  </action>
  <verify>
    <automated>grep -c "upsertProductSynonyms" "O:/AI/E-tic 2026/server/routes/search.ts"</automated>
  </verify>
  <acceptance_criteria>
    - server/lib/typesenseSynonyms.ts exports named const PRODUCT_SYNONYMS (array) and async function upsertProductSynonyms
    - PRODUCT_SYNONYMS contains at least one set whose synonyms include both 'telefon' and 'phone'
    - server/routes/search.ts imports upsertProductSynonyms from '../lib/typesenseSynonyms.js' and calls it inside the reindex handler — grep -c "upsertProductSynonyms" server/routes/search.ts returns 2
    - reindex response object includes a synonyms field
    - All local imports use .js extension
    - tsc --noEmit passes with no new errors in typesenseSynonyms.ts
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                      |
| -------------------------------- | ---------------------------------------------------------------- |
| client → POST /api/search/sync   | Firebase ID token required; ownership verified against Firestore |
| client → POST /api/admin/reindex | Admin role claim required; no seller can call this               |
| server → Typesense Cloud         | Admin API key in process.env; never in browser bundle            |

## STRIDE Threat Register

| Threat ID | Category               | Component               | Disposition | Mitigation Plan                                                                                                            |
| --------- | ---------------------- | ----------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------- |
| T-04-03   | Elevation of Privilege | POST /api/search/sync   | mitigate    | verifyFirebaseToken + sellerId === req.uid ownership check before any Typesense write                                      |
| T-04-04   | Elevation of Privilege | POST /api/admin/reindex | mitigate    | verifyAdmin middleware; sellers cannot call this endpoint                                                                  |
| T-04-05   | Tampering              | productId parameter     | mitigate    | Validate productId is non-empty string; validate action is exactly 'upsert' or 'delete' (allowlist) — reject 400 otherwise |
| T-04-06   | Tampering              | filter_by injection     | accept      | sync endpoint receives productId+action only; no raw filter_by string accepted from client at this layer                   |
| T-04-07   | Information Disclosure | TYPESENSE_ADMIN_API_KEY | mitigate    | Loaded from process.env only; never assigned to VITE\_ prefixed var; not logged                                            |
| T-04-SC   | Tampering              | typesense npm install   | mitigate    | Human-verify checkpoint (Task 1) before install; package [ASSUMED] — verified via npmjs.com + GitHub                       |

</threat_model>

<verification>
After all four tasks:
1. tsc --noEmit — zero new errors
2. grep -c "registerSearchRoutes" server.ts — returns 2 (import + call)
3. grep "PRODUCTS_SCHEMA" server/lib/typesenseSchema.ts — returns the export line
4. grep "default_sorting_field" server/lib/typesenseSchema.ts — returns 'salesCount'
5. node -e "require('./server/lib/typesenseSchema.js').then ? null : null" — no import errors
6. grep -c "upsertProductSynonyms" server/routes/search.ts — returns 2 (import + call); PRODUCT_SYNONYMS includes a telefon/phone set (D-09)
</verification>

<success_criteria>

- typesense package installed
- server/lib/typesenseAdmin.ts and server/lib/typesenseSchema.ts exist with correct exports
- server/routes/search.ts has both endpoints with correct auth guards
- server.ts registers the search routes
- A curated TR/EN synonym set (telefon/phone) is upserted into the products collection during reindex (D-09)
- tsc --noEmit passes
  </success_criteria>

<output>
Create .planning/phases/04-search-discovery/04-02-SUMMARY.md when done
</output>
