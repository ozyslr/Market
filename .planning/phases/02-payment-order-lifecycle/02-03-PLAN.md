---
phase: 02-payment-order-lifecycle
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - package.json
  - server/services/emailService.ts
  - server/services/emailTemplates.ts
  - server/routes/email.ts
  - server.ts
  - src/services/emailService.ts
  - src/services/notificationService.ts
  - .env.example
autonomous: false
requirements: [NOT-01, NOT-02, NOT-03]
user_setup:
  - service: "@sendgrid/mail"
    why: "Transactional email delivery (SMTP)"
    env_vars:
      - name: SENDGRID_API_KEY
        source: "SendGrid Dashboard -> Settings -> API Keys -> Create API Key (Full Access)"
    dashboard_config:
      - task: "Verify sender identity (Single Sender Verification)"
        location: "SendGrid Dashboard -> Settings -> Sender Authentication"
  - service: "resend" (alternative)
    why: "Alternative to SendGrid for transactional emails"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys"
must_haves:
  truths:
    - "Customer receives order confirmation email after successful payment"
    - "Customer receives shipping update email when seller marks Shipped"
    - "Customer receives delivery confirmation email"
    - "Customer receives refund notification email"
    - "Seller receives new order email notification"
    - "Cart abandonment email triggers after 1+ hour inactivity"
  artifacts:
    - path: "server/services/emailService.ts"
      provides: "SendGrid/Resend SMTP email sender"
      min_lines: 80
    - path: "server/services/emailTemplates.ts"
      provides: "React Email HTML templates for all notification types"
      contains: "orderConfirmation"
    - path: "server/routes/email.ts"
      provides: "Email trigger configuration endpoint"
      contains: "POST"
  key_links:
    - from: "server/routes/iyzico.ts (callback)"
      to: "server/services/emailService.ts"
      via: "sendOrderConfirmation on payment success"
      pattern: "sendOrderConfirmation"
    - from: "server/routes/orders.ts (transition)"
      to: "server/services/emailService.ts"
      via: "sendShippingUpdate on mark_shipped"
      pattern: "sendShippingUpdate"
---

<objective>
**As a** customer, **I want to** receive email confirmations for orders, shipping updates, and delivery notifications, **so that** I stay informed about my purchases without checking the site.

Purpose: Replace Firebase Trigger Email with SendGrid/Resend SMTP for production-grade transactional email delivery (per D-10). Create React Email templates for all notification types. Wire email triggers into payment callback and order transition endpoints. Enhance cart abandonment email.

Output: Server email service, React Email templates, trigger integration, enhanced cart abandonment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-payment-order-lifecycle/02-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-foundation-compliance/01-01-SUMMARY.md
@.planning/phases/01-foundation-compliance/01-03-SUMMARY.md

<read_first>
## Key Existing Code

### Email Infrastructure
- **`src/services/emailService.ts`** — client-side service, writes to Firestore `mail` collection (Firebase Trigger Email pattern). Uses hardcoded HTML strings. Must be replaced with server-side SendGrid/Resend.
- **Abandoned cart endpoint** — `POST /api/abandoned-cart/check` in server.ts (~120 lines). Creates inline HTML emails and writes to `mail` collection.

### Trigger Points (where emails must fire)
- **Payment success** — `POST /api/iyzico/callback` in Plan 02 (webhook handler dedup + stock).
- **Order shipped** — `POST transition` endpoint in Plan 02 routes/orders.ts.
- **Order delivered** — Future transition (when customer or system confirms delivery).
- **Order refunded** — Plan 05 refund endpoint.
- **Cart abandonment** — Existing `POST /api/abandoned-cart/check` endpoint in server.ts.

### .env.example
- Current `.env.example` has `SENDGRID_API_KEY` documented.
</read_first>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Email provider decision — SendGrid vs Resend</name>
  <files>.env.example</files>
  <decision>Which transactional email provider to use for production email delivery (per Claude's Discretion in D-10)</decision>
  <context>
    Research (RESEARCH.md) recommends SendGrid (100 emails/day free tier) or Resend (simpler API, 100 emails/month free tier). Both use SMTP. The @sendgrid/mail npm package has ~5M weekly downloads (established). Resend has ~200K weekly downloads (newer, better DX). Firebase Trigger Email (current) must be replaced for production.
  </context>
  <options>
    <option id="sendgrid">
      <name>SendGrid (@sendgrid/mail)</name>
      <pros>100 emails/day free tier; 5M weekly npm downloads; established Turkish deliverability; lower cost at scale</pros>
      <cons>More complex API; sender verification required; slightly slower setup</cons>
    </option>
    <option id="resend">
      <name>Resend (resend)</name>
      <pros>Simpler API (single call); excellent developer experience; react-email native support</pros>
      <cons>100 emails/month free tier (less generous); newer/smaller ecosystem at ~200K weekly downloads</cons>
    </option>
  </options>
  <resume-signal>Select: sendgrid or resend</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install email packages and create server email service + templates</name>
  <files>package.json, server/services/emailService.ts, server/services/emailTemplates.ts</files>
  <read_first>
    src/services/emailService.ts — existing client-side email HTML builder for reference (the HTML template patterns, Turkish strings)
    server.ts — existing abandoned cart email HTML for inline template reference
  </read_first>
  <action>
    **A) Install email packages:**
    Run `npm install @sendgrid/mail` (or `resend` if Resend was chosen in Task 0). Also `npm install react-email @react-email/components`.

    **B) Create `server/services/emailTemplates.ts`:**
    Create a template module with exported functions that return HTML strings (no React-Email runtime — just pure HTML template functions for ESM compatibility):

    - `orderConfirmationHtml(params: { orderSetId: string; customerName: string; items: Array<{ name: string; quantity: number; price: number }>; total: number; currency: string; shippingAddress: any; orderUrl: string })`: Full HTML email with Mercora brand header (#7C3AED purple gradient), item table, totals, shipping address block, and CTA button "Siparisini Takip Et". Model after the existing email HTML pattern in `src/services/emailService.ts` but with better styling.
    
    - `shippingUpdateHtml(params: { orderSetId: string; customerName: string; carrier: string; trackingNumber: string; orderUrl: string })`: Shipping notification with truck icon, carrier name, tracking number, CTA.
    
    - `deliveryConfirmationHtml(params: { orderSetId: string; customerName: string; orderUrl: string })`: Delivery confirmation with checkmark.
    
    - `refundNotificationHtml(params: { orderSetId: string; customerName: string; refundAmount: number; currency: string })`: Refund notification with amount.
    
    - `newSellerOrderHtml(params: { orderSetId: string; sellerName: string; items: Array<{ name: string; quantity: number }>; total: number; currency: string })`: Seller notification of new order with item summary and CTA "Siparisleri Goruntule".
    
    - `abandonedCartHtml(params: { customerName: string; items: Array<{ name: string; image?: string }>; itemCount: number; cartUrl: string })`: Cart recovery with "Sepetinde {N} urun kaldi!" headline, product list, CTA button.
    
    Each template returns a complete HTML document string with inline CSS (consistent with existing email style: `#F8F8FA` background, `#1A1033` dark text, `#7C3AED` brand purple).

    **C) Create `server/services/emailService.ts`:**
    - `sendEmail(params: { to: string; subject: string; html: string; from?: string })`: Uses the selected provider (SendGrid or Resend). If SendGrid: `sgMail.send({ to, from, subject, html })`. If Resend: `resend.emails.send({ from, to, subject, html })`. The from address defaults to `'Benim Olan <noreply@benimolan.com>'`.
    - `sendOrderConfirmationEmail(orderSetId, customerEmail, customerName, items, total, currency, shippingAddress)`: Builds template + calls sendEmail.
    - `sendShippingUpdateEmail(orderSetId, customerEmail, customerName, carrier, trackingNumber)`: Builds template + calls sendEmail.
    - `sendDeliveryConfirmationEmail(orderSetId, customerEmail, customerName)`: Builds template + calls sendEmail.
    - `sendRefundNotificationEmail(orderSetId, customerEmail, customerName, refundAmount, currency)`: Builds template + calls sendEmail.
    - `sendNewSellerOrderEmail(orderSetId, sellerEmail, sellerName, items, total, currency)`: Builds template + calls sendEmail.
    - `sendAbandonedCartEmail(customerEmail, customerName, items, cartUrl)`: Builds template + calls sendEmail.
    - Each function wraps `sendEmail` in try/catch with `logger.error` on failure (non-blocking — email failure should not crash the request).
    - Initialize the provider client at module scope: `const sgMail = process.env.SENDGRID_API_KEY ? (await import('@sendgrid/mail')).default : null`. Similar lazy-init pattern for Resend.

    Commit: `feat(02-03): create email templates and server email service`
  </action>
  <verify>
    <automated>node -e "require('./server/services/emailTemplates.js')" 2>&1 || echo 'Expected ESM import error -- check if tsx can import'; npx tsc --noEmit</automated>
  </verify>
  <done>
    Email packages installed in package.json. server/services/emailTemplates.ts exports 6 template functions. server/services/emailService.ts exports sendEmail and 6 typed convenience senders. TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire email triggers into payment callback, order transition, and cart abandonment</name>
  <files>server/routes/orders.ts, server/routes/iyzico.ts, server/routes/email.ts, src/services/emailService.ts, server.ts</files>
  <read_first>
    server/routes/iyzico.ts — callback handler where payment success triggers email
    server/routes/orders.ts — transition endpoint where mark_shipped triggers email
    server.ts — abandoned cart email endpoint (POST /api/abandoned-cart/check)
    src/services/emailService.ts — client-side service to re-route through server API
  </read_first>
  <action>
    **A) Wire order confirmation email in `server/routes/iyzico.ts`:**
    In the callback handler, after successful transition to 'paid', call `emailService.sendOrderConfirmationEmail(...)` with the OrderSet's userEmail, items, total, and shippingAddress. Non-blocking (try/catch, log failure).

    **B) Wire shipping update email in `server/routes/orders.ts`:**
    In the subOrder transition handler, after successful `mark_shipped` transition, call `emailService.sendShippingUpdateEmail(...)` with the OrderSet's userEmail. Read the OrderSet to get the customer email. Non-blocking.

    **C) Wire seller new-order notification:**
    In the iyzico callback handler, after successful payment processing, send `emailService.sendNewSellerOrderEmail(...)` to each seller's email (retrieve from sellerId -> Firestore users/{sellerId} for email).

    **D) Create `server/routes/email.ts`:**
    - `POST /api/email/trigger-config` (admin-only, verifyAdmin): Simple endpoint to enable/disable specific email types (order confirmation, shipping, delivery, refund, abandonment, seller notification). Store preferences in Firestore `emailConfig` collection.
    - `GET /api/email/trigger-config` (admin-only): Read current config.

    **E) Enhance abandoned cart email in server.ts:**
    Replace the inline email HTML generation in the existing `POST /api/abandoned-cart/check` handler. Instead of writing to Firestore `mail` collection, call `emailService.sendAbandonedCartEmail()` directly. Keep the rest of the business logic (2-hour window check, max 2 reminders, 24h cooldown between sends). Update the window to 1 hour per the phase success criteria (NOT-02: "cart abandonment email triggers after 1 hour of inactivity"). Change `windowHours` default from 2 to 1.

    **F) Update `src/services/emailService.ts` (client-side):**
    Replace the direct Firestore `mail` collection write with a call to the server API `POST /api/email/send` (if needed). Actually, since the email triggers are now server-side (in callback/transition handlers), the client-side service should become a thin wrapper that triggers server-side events. Deprecate the old `sendOrderConfirmationEmail` function — replace it with a no-op or remove it (emails now fire from server callbacks).

    **G) Update `server.ts`:**
    Import `registerEmailRoutes` and register with `{ adminDb, verifyAdmin }`. The route registration must happen after auth middleware creation.

    Commit: `feat(02-03): wire email triggers into payment, order transitions, and cart abandonment`
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Order confirmation email fires on Iyzico payment success. Shipping update email fires on mark_shipped. Seller notification fires on new order. Cart abandonment email triggers after 1-hour window (previously 2h). Email trigger config is admin-manageable. TypeScript compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server -> SendGrid/Resend API | SMTP credentials sent over HTTPS |
| Admin -> email trigger config | Only admin can enable/disable email types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-010 | Information Disclosure | Email content | mitigate | No PII in email subjects; order IDs truncated (#ABC12345 format) |
| T-02-011 | Spoofing | Email from address | mitigate | SPF/DKIM configured via SendGrid sender verification (user_setup step) |
| T-02-012 | Tampering | Email trigger config | mitigate | verifyAdmin on POST /api/email/trigger-config |
| T-02-013 | Denial of Service | Email sending | mitigate | try/catch on every send call — email failure is non-blocking for order processing |
| T-02-SC | Tampering | @sendgrid/mail npm | accept | [ASSUMED] per RESEARCH.md package audit; no new packages beyond sendgrid/resend and react-email |
</threat_model>

<verification>
- `npx tsc --noEmit` — zero errors
- server/services/emailTemplates.ts exports 6 template functions
- server/services/emailService.ts exports typed senders for each notification type
- Cart abandonment email triggers at 1-hour inactivity window (previously 2h)
</verification>

<success_criteria>
- Order confirmation email fires automatically after Iyzico payment success
- Shipping update email fires when seller marks order as Shipped
- Refund notification email fires on refund processing
- Seller receives email when new order is placed
- Cart abandonment email triggers with 1-hour inactivity window
- Email failure does not block order processing (non-blocking try/catch)
</success_criteria>

<output>
Create `.planning/phases/02-payment-order-lifecycle/02-03-SUMMARY.md` when done
</output>
