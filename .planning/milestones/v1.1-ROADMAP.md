# Milestone v1.1: Stabilize & Sharpen

**Status:** ✅ SHIPPED 2026-06-05
**Phases:** 8–11
**Total Plans:** 16
**Git Range:** 0189c11 → ce564cb (59 commits, 87 files, +10,717/−1,574)

## Overview

v1.0 pazar yerini üretime hazır hale getir: admin erişim kontrolü, performans optimizasyonu, satıcı ekleme UX iyileştirmesi ve sertifikalı satın alma hunisi. Yeni büyük özellik yok — sertleştirme ve cila odaklı.

## Phases

### Phase 8: Admin Access Control

**Goal:** Admin yüzeyleri route ve API katmanında rol granularitesi ve tam audit kapsamıyla korunur.
**Depends on:** v1.0 (existing admin pages + custom-claims roles).
**Plans:** 3 plans

Plans:

- [x] 08-01: AdminRole type + requireAdminRole middleware + set-claims bootstrap
- [x] 08-02: AdminRoute client guard (redirect/403 for non-admin)
- [x] 08-03: Audit log coverage — KYC, refunds, payouts, role changes, CMS, data-deletion

**Success Criteria:**

1. Non-admin users cannot reach any `/admin/*` route (client guard redirects/403); verified for buyer, seller, anonymous.
2. Admin roles are granular (super-admin / support / finance) and each admin section enforces the right sub-role.
3. Every sensitive admin action (KYC approve/reject, refund, role change, CMS edit, payout) writes an audit-log entry with actor + timestamp.
4. Every admin API endpoint enforces role server-side (defense-in-depth), independent of the client guard.

### Phase 9: Performance

**Goal:** Measured, budgeted performance with vendor splitting and optimized media.
**Depends on:** v1.0 build pipeline (Vite/PWA/Lighthouse CI already present).
**Plans:** 4 plans

Plans:

- [x] 09-01: manualChunks vendor splitting + bundle budget CI enforcement
- [x] 09-02: Lighthouse CI thresholds hardening (LCP/CLS/TBT on key pages)
- [x] 09-03: Image/CDN optimization (OptimizedImage component, WebP audit script)
- [x] 09-04: Firestore hot-path review (indexes, query efficiency for product lists + search)

**Success Criteria:**

1. `manualChunks` vendor splitting in place; a bundle budget is defined and the analyze report is produced in CI.
2. Lighthouse CI enforces LCP/CLS/TBT thresholds on key pages (home, product detail, checkout) and fails the build on regression.
3. Product media uses optimized/responsive images via a CDN strategy.
4. Hot-path Firestore queries (product lists, search, orders) are reviewed for indexes and read efficiency.

### Phase 10: Seller Add-Flow UX

**Goal:** Lower friction for sellers adding products, especially on mobile.
**Depends on:** v1.0 ProductForm (AI-assisted) + inventory.
**Plans:** 4 plans

Plans:

- [x] 10-01: Quick-add / guided mode (reduced-field fast path)
- [x] 10-02: Mobile-optimized product form (responsive layout, inputs, image upload)
- [x] 10-03: Bulk image upload with drag-reorder (dnd-kit + per-file progress)
- [x] 10-04: Time-to-first-listing instrumentation (seller onboarding funnel)

**Success Criteria:**

1. A quick-add / guided mode lets a seller publish a basic listing with a reduced required-field fast path.
2. The product form is ergonomic on mobile (layout, inputs, image upload).
3. Bulk image upload with drag-reorder works in the product form.
4. Time-to-first-listing is instrumented across the seller onboarding funnel.

### Phase 11: Purchase Funnel & Guest Checkout

**Goal:** A certified, low-friction purchase path including guests, plus closing v1.0 payment UAT.
**Depends on:** v1.0 checkout (Stripe/Iyzico, saved cards, anonymous auth).
**Plans:** 5 plans

Plans:

- [x] 11-01: Guest checkout E2E (localStorage cart, cart merge, guest email, account upgrade)
- [x] 11-02: Address book & autofill (AddressType enum, autocomplete attributes, saved address cards)
- [x] 11-03: Express wallets (Apple Pay / Google Pay via PaymentRequestButtonElement)
- [x] 11-04: Purchase funnel analytics (Firestore event tracking + admin dashboard card)
- [x] 11-05: v1.0 UAT closure (checklist + 3 Playwright E2E tests)

**Success Criteria:**

1. A guest (anonymous) can complete checkout end-to-end to a paid order — verified.
2. Buyers have a saved address book / autofill at checkout.
3. Express wallets (Apple Pay / Google Pay) work via Stripe Payment Request.
4. The purchase funnel (cart → checkout → paid) is instrumented with conversion analytics.
5. v1.0 carried live UAT (payments/3DS, order lifecycle, shipping, reviews/Q&A) is executed and signed off.

---

## Milestone Summary

**Key Decisions:**

| Decision                                                                                               | Rationale                                |
| ------------------------------------------------------------------------------------------------------ | ---------------------------------------- |
| D-ADM-01: AdminRole enum (super-admin \| support \| finance) in Firebase custom claims                 | Granular access without DB schema change |
| D-ADM-02: Non-admin hitting /admin/\* redirected to home with toast                                    | UX clarity over silent 403               |
| D-ADM-03: Audit-log entries for KYC, refunds/payouts, role changes, bans, data-deletion, content edits | Full traceability for compliance         |
| D-ADM-04: Every admin API endpoint passes through verifyAdmin                                          | Defense-in-depth; no client-only guards  |
| D-PERF-04: OptimizedImage srcSet with 3 breakpoints; WebP audit script                                 | Responsive images without CDN dependency |
| D-SLR-01: Quick-add toggle reduces 25+ field form to 6 essential fields                                | Lower seller friction                    |
| D-SLR-02: ProductForm sticky save bar fixed bottom-0 on mobile only                                    | Mobile ergonomics                        |
| D-SLR-03: Bulk image upload with simulated per-file progress + dnd-kit sortable grid                   | Visual feedback for slow uploads         |
| D-BUY-01: Guest email collected at checkout + Stripe Elements + account upgrade prompt                 | Guest-to-registered conversion path      |
| D-BUY-02: localStorage cart persistence + cart merge on sign-in                                        | Anonymous cart survives refresh          |
| D-BUY-03: AddressType enum + HTML autocomplete attributes                                              | Browser-native autofill                  |
| D-BUY-04: PaymentRequestButtonElement for Apple/Google Pay                                             | Native wallet UX                         |
| D-BUY-05: Funnel events in existing events collection with fire-and-forget                             | No new collection needed                 |
| D-BUY-06: UAT checklist + Playwright E2E as verification artifacts                                     | Manual sign-off + automated regression   |

**Technical Debt Incurred:**

- Live UAT sign-off still pending (manual verification needed on production-like environment)
- Phase 04 search Typesense plans on hold — Firestore-only search will need revisit when catalog scales

**Deferred Items:**

- ADM-04 server-side authz parity: verifyAdmin wired to all admin endpoints but exhaustive endpoint audit pending
- SLR-04 seller funnel instrumentation: events recorded but Playwright E2E verification not yet written

---

_For current project status, see .planning/ROADMAP.md_
